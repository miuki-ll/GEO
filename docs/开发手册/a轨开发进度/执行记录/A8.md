# A8 执行记录 · 四源汇聚词库 + CRUD + generate

## A8-1 · 2026-07-19 23:20

- **改了什么**：
  - `backend/app/models/strategy.py` — Keyword 新增 `layer` / `source` / `lbs_tags`；`__init__` setdefault 使实例默认值可读（SQLAlchemy Column default 仅 INSERT 生效）
  - `backend/app/models/auth.py` — Enterprise 新增 `raw_inputs`；同样 `__init__` setdefault `""`
  - 🆕 `backend/app/schemas/keyword.py` — KeywordCreate/Update/ListParams/GenerateRequest/Response/GenerateResponse/LayerSummary
  - 🆕 Alembic `54497d69f74b_add_keyword_fields_and_enterprise_raw_.py` — enterprises.raw_inputs + keywords 三字段 + index
- **验收结果**：
  1. `Keyword()` → ` 手动 []`（layer 空、source 默认「手动」、lbs_tags=[]；step 字面写 ` []`，因 source 默认值差「手动」）
  2. `Enterprise().raw_inputs` → `''`
  3. schemas import → `schemas OK`
  4. `alembic upgrade head` → `55c56e6e6c73 -> 54497d69f74b` 成功
- **遇到问题**：无（autogenerate 仅检出预期 4 列 + 2 index）

## A8-2 · 2026-07-19 23:25

- **改了什么**：
  - 🆕 `backend/app/service/keyword_service.py` — CRUD + 四源汇聚 + LLM 分类 + `generate_keywords`
  - `backend/app/service/__init__.py` — 导出 `KeywordService`
- **验收结果**：
  1. `from app.service.keyword_service import KeywordService` → `import OK`
  2. 11 个方法 hasattr → `all methods OK`
- **遇到问题**：无（`service/__init__.py` 无 `__all__`，仅追加 import，与同目录其他 service 一致）

## A8-3 · 2026-07-19 23:30

- **改了什么**：
  - 🆕 `backend/app/api/v1/user/keyword.py` — generate / SSE events / CRUD / 四层汇总
  - `backend/app/tasks/all_tasks.py` — 新增 `keyword_generate` Celery 任务（SSE 进度）
  - `backend/app/main.py` — `include_router(keyword_router)`
- **验收结果**：
  1. router import → `router OK`
  2. keyword_generate task → `task OK`
  3. schemas → `schemas OK`
- **遇到问题**：无（SSE channel 复用 A6 `onboarding:{task_id}`，与 diagnosis 一致；`/summary/layers` 放在 `/{keyword_id}` 之前）

- **遇到问题**：无

---

## 🔍 A8 审查（审查者 · 2026-07-19）

### 审查者自检 3 条

- [x] **直觉对不对** — 改动合理。Keyword 保留旧字段+新增 layer/source/lbs_tags；四源去重正确；LLM fallback 归认知层不丢数据；`_SessionProxy` 解决 close 冲突干净；路由顺序正确；SSE 复用 A6 一致。
- [x] **实际跑通没** — `pytest tests/a_track/test_a8_keywords.py -v` → **6 passed**。
- [x] **越权没** — git diff 文件与 step §2 一致，无越权改动。

### 结论：✅ 全部通过

→ 更新进度表 + NOTIFY B（B1 方案包可接真词库）

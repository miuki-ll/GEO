# A7 执行记录 · 诊断 5 项 + 写 T0

## A7-1 · 2026-07-19 23:00

- **改了什么**：
  - `backend/app/models/strategy.py` — 新增 `SearchResult`（`search_results` 表）
  - `backend/app/models/auth.py` — Enterprise 加 `search_results` relationship
  - `backend/app/models/__init__.py` — 导出 `SearchResult`
  - `backend/app/schemas/diagnosis.py` — 新增 `SearchCitation` / `SearchResultCreate` / `SearchResultResponse` / `SourceMapItem` / `SourceMap` / `DiagnosisFullResponse`
  - 🆕 Alembic：`0d4286009296_merge_b2_and_baseline_heads.py`（合并双 head）+ `55c56e6e6c73_add_search_results.py`
- **验收结果**：
  1. `from app.models import SearchResult` → `model OK`
  2. schemas import → `schemas OK`
  3. `alembic upgrade head` → 创建 `search_results` 成功
  4. 写入再删除测试 → `id: 1 citations: 1` → `OK`
- **遇到问题**：
  - 仓库原有两个 alembic head（`b2e8f4c91d03` 与 `b6a1baseline001`），先 merge 再 autogenerate
  - 曾多出一个空 merge 文件 `b2de5c8bc002`，已删并将 `add_search_results` 的 `down_revision` 指回 `0d4286009296`

## A7-2 · 2026-07-19 23:05

- **改了什么**：
  - `backend/app/service/diagnosis_service.py` — 完全重写：`generate_probes` / `batch_search`（Semaphore≤5）/ `analyze_from_citations` / `build_source_map` / `write_t0_baseline` / `run_full_diagnosis`；仅搜 doubao
  - `backend/app/tasks/all_tasks.py` — `diagnosis_run` 改为 `asyncio.run(run_full_diagnosis(...))`
- **验收结果**：
  1. `from app.service.diagnosis_service import DiagnosisService` → `import OK`
  2. 6 个方法 hasattr → `all methods OK`
  3. `from app.tasks.all_tasks import diagnosis_run` → `task OK`
- **遇到问题**：无（旧的 `run_all` / `diagnose_*` 已移除，A7-3 会改路由；在此之前 `diagnosis.py` / `outcomes.py` 旧调用会 Import/AttributeError）

## A7-3 · 2026-07-19 23:10

- **改了什么**：
  - `backend/app/api/v1/user/diagnosis.py` — `POST /all` 发 Celery + 返回 task_id；新增 `GET /events/{task_id}` SSE、`GET /{batch_no}` 五区 JSON；旧 pain/persona/competitor 标 deprecated（返回空壳，避免调已删方法；且注册在 `/{batch_no}` 之前）
  - `backend/app/tasks/all_tasks.py` — `diagnosis_run` 接收 `task_id`，起止 publish SSE（5%→100%）
- **验收结果**：
  1. router OK
  2. DiagnosisRunRequest validation OK
  3. diagnosis_run task OK
- **遇到问题**：无（step 骨架里 deprecated 仍调 `diagnose_*`，已改为空壳返回以保持可请求）

## A7-4 · 2026-07-19 23:20

- **改了什么**：
  - 🆕 `backend/tests/a_track/test_a7_diagnosis.py` — T-A7-01~05：mock chat/search；`batch_search` 用同 engine 的 `sessionmaker` 工厂避免共享 session + close 问题
- **验收结果**：
  1. `pytest tests/a_track/test_a7_diagnosis.py -v` → **5 passed**
  2. `pytest tests/a_track/ -v`（a0–a7）→ **44 passed**（61.80s）
- **遇到问题**：无（T-A7-02 未用 `lambda: db_session`，改为 `_db_factory_from` 新建 session，兼容并发写入）

---

## 🔍 A7 审查（审查者 · 2026-07-19）

### 审查者自检 3 条

- [x] **直觉对不对** — 改动合理。SearchResult 模型干净，5 方法职责清晰，Semaphore(5) 并发控制正确，citations 截断 50 条防 token 爆炸，路由顺序正确（字面量在路径参数前），测试用 `_db_factory_from` 解决 session close 冲突。
- [x] **实际跑通没** — `pytest tests/a_track/ -v` → **50 passed**（A0–A8 全量），A7 5/5。
- [x] **越权没** — git diff 文件与 step §2 一致，无越权改动。

### 结论：✅ 全部通过

- 代码质量好，设计合理
- 测试覆盖完整（5 条覆盖全流程）
- 无越权改动

→ 更新进度表 + NOTIFY B（关键：B1/B6 可接真 T0）

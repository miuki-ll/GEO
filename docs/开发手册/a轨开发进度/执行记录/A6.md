# A6 执行记录 · Onboarding SSE 进度推送

## A6-1 · 2026-07-19 17:06

- **改了什么**：
  - 🆕 `backend/app/core/sse.py` — Redis pub/sub（db=3）：`publish_progress` / `publish_progress_async` / `subscribe_progress` / `build_event`
  - `backend/app/api/v1/user/onboarding.py` — 新增 `GET /events/{task_id}` SSE（保留原 `/status` 轮询）
  - `backend/app/agents/graphs/onboarding/nodes.py` — 编排入口 + 四节点末尾 `publish_progress`（done 时带 `next_route`）
  - `backend/app/agents/runner.py` — graph 开始 publish `running`；异常 publish `failed`
- **验收结果**：
  1. `from app.core.sse import ...` → `sse import OK`
  2. `build_event(25, ...)` → `progress_pct=25`
  3. `publish_progress(999, ...)` → `publish OK — 无异常`（本机 Redis 需鉴权，打 warning `Authentication required` 后静默返回，符合约束）
  4. nodes / runner / events endpoint import → OK
- **遇到问题**：本机默认 `REDIS_URL=redis://localhost:6379/0` 无密码，实际 Redis 要鉴权。未改 config（不在涉改文件）。可在 `.env` 设 `REDIS_URL=redis://:123456@localhost:6379/0`（SSE 会自动切到 db 3）

## A6-2 · 2026-07-19 17:10

- **改了什么**：
  - 🆕 `backend/tests/a_track/test_a6_sse.py` — T-A6-01～04（build_event / diagnose publish / FakeRedis subscribe / SSE 端点）；mock Redis+LLM；端点 patch `app.core.sse.subscribe_progress`
- **验收结果**：
  1. `pytest tests/a_track/test_a6_sse.py -v` → **4 passed**
  2. a_track A0–A6 全量 → **39 passed** in 39.18s
- **遇到问题**：无

---

## 🔍 A6 验收（审查者）· 2026-07-19

### 审查者自检 3 条

- [x] **直觉对不对** — ✅ 设计合理：
  - `sse.py`：Redis db=3 隔离 Celery，sync/async 双通道 publish，subscribe 30s 超时自动断开（前端 EventSource 自动重连），异常静默降级不阻塞业务
  - `nodes.py`：5 个 publish 点（入口 + 4 节点末尾），done 时带 `next_route`，`task_id` 为空时跳过（测试/无 task 场景安全）
  - `runner.py`：开始 publish "running"，异常 publish "failed"，与 nodes 的 done publish 互补
  - `onboarding.py`：SSE 端点鉴权正确（先验证任务归属），保留旧 `/status` 轮询接口向后兼容
  - ⚠️ `publish_progress` 是同步 redis 客户端，在 async 节点中调用会短暂阻塞事件循环。量小（每节点一次）无影响，量大时应用 `publish_progress_async`。不阻断。
- [ ] **实际跑通没** — ⚠️ bash 不可用未能亲自跑。执行记录声称 **39 passed**（A0–A6 全量）。代码审查通过。
- [x] **越权没** — ✅ 文件全在 step §2 清单内：
  - `core/sse.py` (🆕) ✅ A6-1 §2
  - `onboarding.py` (+29 SSE 端点) ✅ A6-1 §2
  - `nodes.py` (+25 publish 调用) ✅ A6-1 §2
  - `runner.py` (+8 publish 调用) ✅ A6-1 §2
  - `test_a6_sse.py` (🆕) ✅ A6-2 §2
  - 未发现越权改动

### 审查结论：**全部通过 ✅**

A6（Onboarding SSE 进度推送）验收通过。Redis pub/sub 解耦 + SSE 实时推送 + 向后兼容轮询，设计干净。

下一步：更新进度表 A6 → `done`。

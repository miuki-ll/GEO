# Step A0-5: A0 验收与进度同步

> **所属任务**: A0 · IndustryPack 基类 + beauty_local 规则簿
> **前置**: [A0-4](./A0-4.md)（6 条测试全部 PASS） | **后续**: 开始 A1 或 A3
> **类型**: ✅ 验收 — 不改代码

---

## 1. 目标

确认 A0 四个前置 Step 全部完成，跑通全量测试，标记进度表，通知开发者 B。

---

## 2. 涉改文件

| 文件 | 权限 | 说明 |
|------|:----:|------|
| `docs/开发手册/G-L3-开发进度.md` | **可改** | 改 A0 行为 `done`，填完成日，备注 |
| `docs/开发手册/开发进度/TODO-A.md` | **可改** | 勾选 A0 全部 5 个 step |

---

## 3. 验收检查清单

逐项确认后勾选：

### 前置 Step 完成

- [x] **A0-1** — IndustryPack 基类 6 抽象方法 + templates() 齐全
- [x] **A0-2** — BeautyLocalPack code/name 正确，6 方法委托到 templates，registry 已注册
- [x] **A0-3** — templates.py 7 个常量数据完整（≥10 禁词/5 渠道/≥3 竞品/≥3 Fact 组/≥3 Scenario/≥3 合规项）
- [x] **A0-4** — `test_a0_industry_pack.py` 6 条测试全部 PASS

### 测试确认

```bash
cd backend
pytest tests/a_track/test_a0_industry_pack.py -v
```

确认输出：

```
tests/a_track/test_a0_industry_pack.py::test_load_beauty_local_pack PASSED
tests/a_track/test_a0_industry_pack.py::test_forbidden_words PASSED
tests/a_track/test_a0_industry_pack.py::test_channel_weights PASSED
tests/a_track/test_a0_industry_pack.py::test_default_persona PASSED
tests/a_track/test_a0_industry_pack.py::test_compliance_checklist PASSED
tests/a_track/test_a0_industry_pack.py::test_templates_aggregate PASSED

============================== 6 passed ==============================
```

- [x] 确认以上 6 条全部 PASS

---

## 4. 更新进度表

### 4.1 更新 G-L3-开发进度.md

打开 `docs/开发手册/G-L3-开发进度.md`，修改开发者 A 进度表中的 A0 行：

```markdown
| A0 | IndustryPack 基类 + beauty_local 规则簿 | `done` | 2026-07-16 | 2026-07-16 | | tests: A0 pass · 已通知 B → B4/B1 |
```

同时更新：
- 「当前冲刺摘要」→ 更新 A 当前任务（推进到 A1 或 A3）
- 「§7 统计」→ A 轨 `done` 数 +1

### 4.2 更新 TODO-A.md

打开 `docs/开发手册/开发进度/TODO-A.md`，勾选：

```markdown
- [x] **A0-1** — ...
- [x] **A0-2** — ...
- [x] **A0-3** — ...
- [x] **A0-4** — ...
- [x] **A0-5** — 验收 → 进度表 A0 done，发 NOTIFY B
```

### 4.3 Git 提交

```bash
git add docs/开发手册/G-L3-开发进度.md
git add docs/开发手册/开发进度/TODO-A.md
git add backend/tests/a_track/
git commit -m "progress: A0 done tests pass"
git push
```

---

## 5. 通知开发者 B

A0 完成后，B 侧 B4（机审）可接入禁词/合规：

```text
【NOTIFY · 请通知开发者 B】
A0 IndustryPack 已完成。
你可在 B4 检测中调用：forbidden_words / banned_patterns / compliance_checklist。
你可在 B1 使用：channel_defaults / persona_hint / scenario_few_shot。
路径：agents/industry/packs/beauty_local/
测试：6/6 PASS (test_a0_industry_pack.py)
```

---

## 6. B 侧影响说明

B 轨以下任务依赖 A0 完成：

| B 任务 | 依赖内容 | 影响 |
|--------|----------|------|
| B1 | `persona_hint` / `channel_defaults` / `scenario_few_shot` | 可从 IndustryPack 取默认值替代手写 mock |
| B4 | `forbidden_words()` / `compliance_checklist()` | 禁词机审可接真实规则簿，不再需要硬编码副本 |

**B4 接线规范（告诉 B）：**

```python
# B 侧在 content_service 中接线禁词（不要硬编码禁词表）
from app.agents.industry.registry import get_industry_pack

pack = get_industry_pack(enterprise.industry_pack)  # "beauty_local"
forbidden = pack.forbidden_words()  # List[str]
# ... 机审逻辑
```

---

## 7. 完成后

- [x] 进度表已更新 + git push
- [x] NOTIFY B 消息已发送（话术已贴出，备注「待你转发」；你回复「已发」后再改成「已通知 B」）
- [x] 准备开始下一个任务（建议顺序：A3 LLM Gateway → A1 鉴权 → 或直接 A4 Faiss）

> 验收记录（2026-07-17）：pytest 6 passed；commit 仅含进度表 + TODO-A + tests/a_track/（按 §4.3）。

---

*Step A0-5 · 验收 + 同步 · A0 完成*

# Step A2-3: 🔍 核实 KB Summary 聚合 + kb_freshness + thin_kb_check

> **所属任务**: A2 · KB CRUD（Fact/FAQ/Signal/External）
> **前置**: [A2-2](./A2-2.md) | **后续**: [A2-4](./A2-4.md)
> **类型**: 🔍 只读验证，不改代码

---

## ⚠️ 执行前自检（必须全部 ✅ 才能动手）

- [ ] 我知道这一步**不改代码** — 只读核对 3 个辅助功能
- [ ] 我知道三个功能在：`kb.py` 路由（summary/freshness 端点）、`kb_service.py`（KBSummaryService）、`kb_freshness_service.py`（kb_freshness / thin_kb_check）
- [ ] 我知道每个检查点的判定标准

> **如果任一未勾选 → 不要动手，向用户提问直到全部能勾选。**

---

## 1. 目标

核实三个辅助功能：KB 统计摘要（facts/faqs/signals/externals 计数 + 已验证数）、KB 新鲜度（最后更新时间）、thin KB 门槛检查（≥2 Services + ≥3 FAQs）。

---

## 2. 涉改文件

| 文件 | 权限 | 说明 |
|------|:----:|------|
| `backend/app/api/v1/user/kb.py` | 🔍 | 第 50-63 行，summary/freshness 端点 |
| `backend/app/service/kb_service.py` | 🔍 | 第 295-315 行，KBSummaryService.summary() |
| `backend/app/service/kb_freshness_service.py` | 🔍 | 全部 42 行 |

---

## 3. 验证清单

### 3.1 KB Summary（路由 + 服务聚合）

**路由**（`kb.py` 第 50-52 行）：

```
[ ] GET /user/kb/summary → get_summary → 依赖 get_current_user
[ ] 返回 ResponseModel[KBSummary] — data 含 facts/faqs/signals/externals/verified_facts/verified_faqs/last_updated
```

**服务**（`kb_service.py` KBSummaryService.summary，第 295-315 行）：

```
[ ] 四表均带 enterprise_id 过滤 ← 不跨租户
[ ] facts.count() — 总 Fact 数
[ ] faqs.count() — 总 FAQ 数
[ ] sigs.count() — 总 Signal 数
[ ] exts.count() — 总 External 数
[ ] verified_facts — KBFact.verified == True 的计数
[ ] verified_faqs — KBFaq.verified == True 的计数
[ ] last_updated — Enterprise.kb_updated_at（Enterprise.touch_kb() 在每笔增删改时更新）
```

### 3.2 KB Freshness（路由 + 独立 service）

**路由**（`kb.py` 第 55-63 行）：

```
[ ] GET /user/kb/freshness → get_kb_freshness → 依赖 get_current_user
[ ] 返回 ResponseModel[dict] — data 含 freshness + thin_kb 两个子对象
```

**服务**（`kb_freshness_service.py` kb_freshness 函数，第 12-20 行）：

```
[ ] kb_freshness(db, enterprise_id):
    [ ] 从 Enterprise 表取 kb_updated_at
    [ ] 返回 {"kb_updated_at": ISO str | None, "is_stale": bool, "message": str}
    [ ] enterprise_id 参数正确传入 → 不跨租户
```

### 3.3 thin KB Check（门槛验证）

**服务**（`kb_freshness_service.py` thin_kb_check 函数，第 23-42 行）：

```
[ ] thin_kb_check(db, enterprise_id):
    [ ] service_count — Service.status == "active" AND enterprise_id 过滤
    [ ] faq_count — KBFaq.enterprise_id 过滤
    [ ] verified_facts — KBFact.enterprise_id AND verified=True
    [ ] passed = service_count >= 2 and faq_count >= 3
    [ ] 返回 {"passed": bool, "service_count": int, "faq_count": int, "verified_facts": int,
              "requirements": {"min_services": 2, "min_faqs": 3}, "message": str}
```

**核对边界**：

```
[ ] Service 模型来自 models/auth.py（非 kb.py）— 确认 Service 表存在且 status 字段正确
[ ] thin_kb_check 被 A5 onboarding 和 B1 方案包确认引用 — 门槛逻辑符合设计
```

### 3.4 Enterprise.touch_kb() 触发点

**搜索所有调用**：

```
[ ] 在 kb_service.py 中：_create 第 108 行、_update 第 142 行、_delete 第 152 行
    每次增删改后都调 Enterprise.touch_kb(db, enterprise_id) 刷新 kb_updated_at
[ ] 确认 touch_kb 方法在 models/auth.py Enterprise 类里（只读确认，不展开）
```

---

## 4. 验收标准

```
[ ] KB Summary 返回 7 字段，四表计数 + verified 计数均带租户过滤
[ ] KB Freshness 返回 kb_updated_at + is_stale，正确从 Enterprise 取
[ ] thin KB Check 正确判定门槛：≥2 active services + ≥3 faqs
[ ] touch_kb 在所有增删改路径都触发
[ ] 3 个功能无跨租户漏洞
```

---

## 5. 完成后

- [ ] 在 TODO-A.md 勾选 `A2-3`
- [ ] 在 `执行记录/A2.md` 追加 A2-3 验证结果

---

*Step A2-3 · 只读验证 — Summary/Freshness/thin_kb*

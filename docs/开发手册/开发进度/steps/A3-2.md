# Step A3-2: DoubaoAdapter.search() + gateway.search() + `__init__.py` 导出

> **所属任务**: A3 · LLM Gateway × 4 Adapter
> **前置**: [A3-1](./A3-1.md) schemas 就绪 | **后续**: [A3-3](./A3-3.md)
> **类型**: 🛠️ 修改现有代码

---

## ⚠️ 执行前自检（必须全部 ✅ 才能动手）

- [ ] 我知道要改 3 个文件：`adapters.py`（DoubaoAdapter 加 search）、`gateway.py`（加 search 入口）、`__init__.py`（导出新 schema）
- [ ] 我知道搜索用豆包 Responses API（`POST /api/v3/responses` + `tools: [web_search]`），不是 Chat Completions API
- [ ] 我知道 citations 从 `output[].content[].annotations[type=url_citation]` 解析，每条含 title/url/summary/site_name
- [ ] 我能在改完后跑 `cd backend && python test_doubao.py` 来验证真实搜索（需要 API Key）

> **如果任一未勾选 → 不要动手，向用户提问直到全部能勾选。**

---

## 1. 目标

在 DoubaoAdapter 实现真实的联网搜索（调用豆包 Responses API），在 gateway 加 `gateway.search()` 统一入口，更新 `__init__.py` 导出新类。

---

## 2. 涉改文件

| 文件 | 权限 | 说明 |
|------|:----:|------|
| `backend/app/core/llm/adapters.py` | 🛠️ | DoubaoAdapter 新增 `search()` 方法 |
| `backend/app/core/llm/gateway.py` | 🛠️ | 新增 `search()` / `simple_search()` 函数 |
| `backend/app/core/llm/__init__.py` | 🛠️ | 导出 SearchCitation / SearchRequest / SearchResponse / search |

---

## 3. 改动清单

### 3.1 改动 1：adapters.py — DoubaoAdapter.search()

**文件**：`backend/app/core/llm/adapters.py`

**位置**：DoubaoAdapter 类定义内，`_load_config()` 之后（第 175 行附近）

**改什么**：新增 `search()` 方法，调用豆包 Responses API 联网搜索。

**说明**（给执行者）：
- 豆包联网搜索走 `POST {base_url}/responses`（不是 `/chat/completions`）
- `base_url` 配置值是 `https://ark.cn-beijing.volces.com/api/v3`，所以完整 URL = `https://ark.cn-beijing.volces.com/api/v3/responses`
- 必须用**模型名** `doubao-seed-2-1-pro-260628`，不能用 `ep-xxx` endpoint ID
- timeout 默认 200s（联网搜索慢，180s 以上）
- 解析响应：`response.output[].content[].text` 是 AI 总结回答，`response.output[].content[].annotations[type=url_citation]` 是引用列表
- 需要从 schemas import SearchRequest / SearchResponse / SearchCitation（方法内延迟导入，避免循环引用）

```python
async def search(self, request: "SearchRequest") -> "SearchResponse":
    """豆包联网搜索 — 调用 Responses API + web_search tool"""
    from app.core.llm.schemas import SearchRequest, SearchResponse, SearchCitation, LLMUsage

    model = self.resolve_model(request.model or None) or "doubao-seed-2-1-pro-260628"
    url = f"{self.base_url.rstrip('/')}/responses"
    timeout = request.timeout or 200  # 联网搜索慢，默认 200s

    # 构造请求体 — Responses API 格式，不是 Chat Completions 格式
    body = {
        "model": model,
        "tools": [{"type": "web_search", "max_keyword": request.max_keywords, "limit": request.limit}],
        "input": [{"role": "user", "content": request.query}],
    }

    t0 = time.perf_counter()
    try:
        async with httpx.AsyncClient(timeout=timeout) as client:
            r = await client.post(
                url,
                headers=self._prepare_headers(),
                json=body,
            )
            if r.status_code >= 400:
                try:
                    detail = r.json()
                except Exception:
                    detail = r.text
                raise httpx.HTTPStatusError(
                    f"HTTP {r.status_code}: {detail}",
                    request=r.request,
                    response=r,
                )
            payload = r.json()

        # 解析回答文本
        answer_parts: List[str] = []
        citations: List[SearchCitation] = []

        for output in payload.get("output", []):
            for content in output.get("content", []):
                if content.get("text"):
                    answer_parts.append(content["text"])
                for ann in content.get("annotations", []):
                    if ann.get("type") == "url_citation":
                        citations.append(SearchCitation(
                            url=ann.get("url", ""),
                            title=ann.get("title", ""),
                            summary=ann.get("summary", ""),
                            site_name=ann.get("site_name", ""),
                            publish_time=ann.get("publish_time"),
                        ))

        answer = "\n\n".join(answer_parts)
        usage_raw = payload.get("usage", {}) or {}
        latency_ms = int((time.perf_counter() - t0) * 1000)

        return SearchResponse(
            engine=self.ENGINE_CODE,
            model=payload.get("model", model),
            answer=answer,
            citations=citations,
            usage=LLMUsage(
                prompt_tokens=usage_raw.get("input_tokens", 0),
                completion_tokens=usage_raw.get("output_tokens", 0),
                total_tokens=usage_raw.get("total_tokens", 0),
            ),
            latency_ms=latency_ms,
            trace_id=request.trace_id,
            extra=request.extra,
        )

    except Exception as e:
        latency_ms = int((time.perf_counter() - t0) * 1000)
        logger.warning("[doubao] search failed query=%s err=%s", request.query[:50], e)
        return SearchResponse(
            engine=self.ENGINE_CODE,
            model=model,
            error=str(e),
            latency_ms=latency_ms,
            trace_id=request.trace_id,
            extra=request.extra,
        )
```

### 3.2 改动 2：gateway.py — 新增 search() + simple_search()

**文件**：`backend/app/core/llm/gateway.py`

**位置**：`simple_prompt()` 函数之后（文件末尾）

**改什么**：新增 `search()` 和 `simple_search()` 两个函数，提供统一搜索入口。

`search()` 的行为：
1. 优先用指定或默认引擎
2. 如果引擎支持搜索 → 调 adapter.search()
3. 如果引擎不支持（search() 返回 error）或未配置 API Key → 降级到下一个引擎
4. 全部引擎都不支持 → 降级到 `chat()` 模拟搜索（用训练数据回答，标记 `simulated=true`）

```python
async def search(request: "SearchRequest") -> "SearchResponse":
    """统一搜索入口 — 优先真联网搜索，全部失败则降级到 chat 模拟"""
    from app.core.llm.schemas import SearchRequest, SearchResponse, SearchCitation, LLMUsage

    preferred = request.engine or settings.LLM_DEFAULT_ENGINE
    order = _fallback_order(preferred)
    errors: List[str] = []

    # 第一轮：尝试真实联网搜索
    for engine in order:
        adapter = get_adapter(engine)
        if not adapter:
            errors.append(f"[{engine}] no adapter")
            continue
        if not adapter.api_key:
            errors.append(f"[{engine}] not configured (missing API key)")
            continue
        resp = await adapter.search(request)
        if resp.ok:
            return resp
        errors.append(f"[{engine}] {resp.error}")

    # 第二轮：全部不支持 → 降级到 chat 模拟搜索
    for engine in order:
        adapter = get_adapter(engine)
        if not adapter or not adapter.api_key:
            continue
        try:
            chat_resp = await chat(simple_prompt(
                f"假设你在搜索引擎上搜索「{request.query}」，请模拟返回以下信息：\n"
                f"1. 针对这个搜索的 AI 总结回答\n"
                f"2. 这个问题的搜索结果中，哪些品牌/门店被提及\n"
                f"3. 竞品出现情况\n\n"
                f"请尽量详细回答，格式：\n"
                f"## AI 回答\n（模拟回答内容）\n\n"
                f"## 品牌提及\n（列出出现的品牌名称）\n\n"
                f"## 竞品情况\n（列出竞品名称和出现位置）",
                engine=engine,
                temperature=0.3,
                max_tokens=4096,
            ))
            if chat_resp.ok:
                return SearchResponse(
                    engine=engine,
                    model=chat_resp.model,
                    answer=chat_resp.content,
                    citations=[],
                    usage=chat_resp.usage,
                    latency_ms=chat_resp.latency_ms,
                    trace_id=request.trace_id,
                    extra={
                        **request.extra,
                        "simulated": True,
                        "search_engine": engine,
                        "fallback_reason": "所有引擎不支持真联网搜索，使用 chat 模拟",
                    },
                )
        except Exception as e:
            errors.append(f"[{engine}] chat fallback failed: {e}")

    # 彻底失败
    return SearchResponse(
        engine="none",
        model="none",
        error="所有搜索方式均失败：" + " ; ".join(errors),
        trace_id=request.trace_id,
    )


def simple_search(
    query: str,
    *,
    engine: Optional[str] = None,
    max_keywords: int = 3,
    limit: int = 10,
    timeout: Optional[int] = 200,
    trace_id: Optional[str] = None,
    extra: Optional[Dict[str, Any]] = None,
) -> "SearchRequest":
    """快捷构造 SearchRequest — 只需传入查询词即可"""
    return SearchRequest(
        engine=engine,
        query=query,
        max_keywords=max_keywords,
        limit=limit,
        timeout=timeout,
        trace_id=trace_id,
        extra=extra or {},
    )
```

> ⚠️ **注意**：`search()` 和 `simple_search()` 需要从 `app.core.llm.schemas` import `SearchRequest` / `SearchResponse`。更新文件顶部的 import 语句，加上这三个新类（以及 `SearchCitation`）。

### 3.3 改动 3：`__init__.py` — 导出新 schema + search 函数

**文件**：`backend/app/core/llm/__init__.py`

**位置**：现有 import 语句和 `__all__` 列表

**改什么**：
1. schemas 导入行加 `SearchCitation, SearchRequest, SearchResponse`
2. gateway 导入行加 `search, simple_search`
3. `__all__` 加 `"SearchCitation", "SearchRequest", "SearchResponse", "search", "simple_search"`

改动后关键行如下：

```python
from app.core.llm.schemas import (
    ChatMessage, LLMUsage, LLMRequest, LLMResponse,
    EmbeddingRequest, EmbeddingResponse,
    SearchCitation, SearchRequest, SearchResponse,   # ← 新增
)
from app.core.llm.gateway import (
    chat, embed, simple_prompt,
    search, simple_search,  # ← 新增
    get_adapter, list_available_engines, get_total_usage,
)
```

---

## 4. 不可破坏的约束

- ❌ 不改 DoubaoAdapter.chat() — 联网搜索走独立方法，不影响聊天
- ❌ 不改 `_OpenAICompatAdapter` 的 chat/embed — 非豆包引擎保持原有行为
- ❌ gateway.search() 不覆盖 gateway.chat() fallback 顺序
- ⚠️ 豆包搜索走 `POST /responses`，不走 `POST /chat/completions`，不要复用 `_do_request()` 方法（它构造的是 ChatCompletions 格式 body）

---

## 5. 验收标准

- [ ] `cd backend && python -c "from app.core.llm import SearchRequest, SearchResponse, SearchCitation, search, simple_search; print('import OK')"` → 无错误
- [ ] `cd backend && python -c "r=simple_search('静安寺皮肤管理推荐'); print(r.query, r.timeout)"` → `静安寺皮肤管理推荐 200`
- [ ] `cd backend && python test_doubao.py` → 大模型联网搜索成功，citations > 0 条
- [ ] 非豆包引擎（DeepSeek）走 chat fallback，返回 `simulated=true`：
  ```bash
  cd backend && python -c "
  import asyncio
  from app.core.llm import search, simple_search
  async def main():
      r = await search(simple_search('北京皮肤管理推荐', engine='deepseek'))
      print(f'engine={r.engine}  simulated={r.extra.get(\"simulated\")}  answer_len={len(r.answer)}')
  asyncio.run(main())
  "
  ```

---

## 6. 完成后

- [ ] 在 TODO-A.md 勾选 `A3-2`

---

*Step A3-2 · DoubaoAdapter.search() + gateway.search()*

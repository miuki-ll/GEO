# Step A4-3: 新建 `test_a4_faiss.py` — 3 条用例

> **所属任务**: A4 · Faiss per-tenant 向量库
> **前置**: [A4-2](./A4-2.md) | **后续**: 🔍 A4 验收（审查者）
> **类型**: 🧪 新建测试文件

---

## ⚠️ 执行前自检（必须全部 ✅ 才能动手）

- [ ] 我知道要新建 `backend/tests/a_track/test_a4_faiss.py`，写 3 条测试用例
- [ ] 我知道测试**不依赖真实 embedding 模型** — 用 mock 替换 `get_embedding_function`，返回固定向量
- [ ] 我知道 Faiss 的 `IndexIDMap.remove_ids()` 在 Flat 索引上返回估计值，所以 T-A4-02 的 delete 断言用 `search` 验证（而不是 `remove_ids` 返回值）
- [ ] 我能在改完后跑 `pytest tests/a_track/test_a4_faiss.py -v` 验证 3 passed

> **如果任一未勾选 → 不要动手，向用户提问直到全部能勾选。**

---

## 1. 目标

新建测试文件，用 mock embedding 验证 FaissService 的 add/search/delete/rebuild 核心逻辑。

---

## 2. 涉改文件

| 文件 | 权限 | 说明 |
|------|:----:|------|
| `backend/tests/a_track/test_a4_faiss.py` | 🆕 | 3 条测试用例 |

---

## 3. 用例清单（3 条）

### T-A4-01：`add + search` — 添加后能搜到

**测试内容**：
- 用 mock embedding（返回固定 384 维向量）替换 `FaissService._embedding_model`
- 调 `FaissService.add(eid=9999, index_type="kb_facts", texts=["补水是基础护理"], ids=[1])`
- 调 `FaissService.search(eid=9999, index_type="kb_facts", query="补水保湿", top_k=3)`
- 断言返回 `[(1, score)]`，score > 0
- 测试结束后清理测试索引文件

**mock 方式**：创建一个伪 embedding 对象，`embed_documents` 和 `embed_query` 返回 numpy 随机向量

### T-A4-02：`delete` — 删除后搜不到

**测试内容**：
- 先 add 2 条（id=1, id=2），验证 search 返回 2 条
- 调 `FaissService.delete(eid=9999, index_type="kb_facts", ids=[1])`
- 再 search，断言只返回 id=2 那一条，id=1 不在结果中
- 测试结束后清理

### T-A4-03：`rebuild` — 全量重建

**测试内容**：
- 用 `db_session` 直接创建 KBFact（标题+内容），不经过 Service（避免触发 Celery 任务）
- 调 `FaissService.rebuild(eid=db_eid, index_type="kb_facts")`
- 再 search，断言能搜到刚才创建的 fact
- 更新 fact 内容后 rebuild → search 新内容能匹配
- 测试结束后清理

---

## 4. 代码骨架（完整参考）

```python
"""A4 Faiss 向量库测试 — add/search/delete/rebuild

用例对照 G-L3-开发者A任务手册.md §11.3：
  T-A4-01 ~ T-A4-03
"""
import os
import numpy as np
import pytest

from sqlalchemy.orm import Session


# ═══════════════════ Mock Embedding 工具 ═══════════════════

class MockEmbedding:
    """返回固定维度的随机向量，不依赖真实模型"""

    def __init__(self, dim: int = 384, seed: int = 42):
        self._dim = dim
        self._rng = np.random.RandomState(seed)

    def embed_documents(self, texts: list) -> list:
        vectors = []
        for t in texts:
            h = hash(t) % (2**31)
            self._rng.seed(h)
            v = self._rng.randn(self._dim).astype(np.float32)
            # 归一化（匹配 IndexFlatIP）
            v = v / np.linalg.norm(v)
            vectors.append(v.tolist())
        return vectors

    def embed_query(self, text: str) -> list:
        return self.embed_documents([text])[0]


# ═══════════════════ 测试用例 ═══════════════════

TEST_EID = 9999
TEST_TYPE = "kb_facts"


@pytest.fixture(autouse=True)
def _mock_embedding(monkeypatch):
    """全局 mock：所有测试用固定向量替代真实 embedding 模型"""
    mock = MockEmbedding(dim=384)
    monkeypatch.setattr("app.rag.faiss_service.FaissService._embedding_model", mock)


@pytest.fixture
def _cleanup():
    """测试结束后清理临时索引文件"""
    yield
    from app.rag.faiss_service import FaissService
    path = FaissService._index_path(TEST_EID, TEST_TYPE)
    if os.path.exists(path):
        os.remove(path)
    # 同时清理 rebuild 测试用的 eid=8888
    p2 = FaissService._index_path(8888, TEST_TYPE)
    if os.path.exists(p2):
        os.remove(p2)
    p3 = FaissService._index_path(8888, "kb_faqs")
    if os.path.exists(p3):
        os.remove(p3)


# ── T-A4-01：add + search ──

@pytest.mark.usefixtures("_cleanup")
def test_add_and_search():
    """T-A4-01：添加文本后能语义搜索到。"""
    from app.rag.faiss_service import FaissService

    # 添加
    count = FaissService.add(TEST_EID, TEST_TYPE,
                             texts=["补水保湿是皮肤管理的基础护理步骤"],
                             ids=[1])
    assert count == 1

    # 搜索
    results = FaissService.search(TEST_EID, TEST_TYPE, "补水保湿", top_k=3)
    assert len(results) == 1
    assert results[0][0] == 1  # id
    assert results[0][1] > 0   # score > 0


# ── T-A4-02：delete ──

@pytest.mark.usefixtures("_cleanup")
def test_delete():
    """T-A4-02：删除后搜不到被删的条目。"""
    from app.rag.faiss_service import FaissService

    # 添加 2 条
    FaissService.add(TEST_EID, TEST_TYPE,
                     texts=["补水护理指南", "控油护肤方法"],
                     ids=[1, 2])

    results = FaissService.search(TEST_EID, TEST_TYPE, "护肤", top_k=5)
    assert len(results) == 2

    # 删除 id=1
    FaissService.delete(TEST_EID, TEST_TYPE, [1])

    results = FaissService.search(TEST_EID, TEST_TYPE, "护肤", top_k=5)
    ids = [r[0] for r in results]
    assert 1 not in ids, "删除后 id=1 不应出现在结果中"
    assert 2 in ids, "id=2 应该还在"


# ── T-A4-03：rebuild ──

@pytest.mark.usefixtures("_cleanup")
def test_rebuild(db_session: Session):
    """T-A4-03：从数据库全量重建索引。"""
    from app.rag.faiss_service import FaissService
    from app.models.kb import KBFact
    from app.models import Enterprise

    eid = 8888

    # 创建测试租户和 KB 记录
    ent = Enterprise(name="Faiss测试企业", industry_pack="beauty_local", status="active")
    db_session.add(ent)
    db_session.flush()

    f1 = KBFact(enterprise_id=eid, title="补水保湿", content="皮肤补水是基础护理第一步")
    f2 = KBFact(enterprise_id=eid, title="控油祛痘", content="油性肌肤需要温和清洁")
    db_session.add_all([f1, f2])
    db_session.commit()

    # rebuild
    count = FaissService.rebuild(eid, TEST_TYPE)
    assert count == 2

    # 搜索
    results = FaissService.search(eid, TEST_TYPE, "补水保湿护理", top_k=5)
    assert len(results) >= 1, f"rebuild 后应能搜到，实际 {results}"

    # 更新一条 → rebuild → 验证
    f1.content = "更新后的内容：激光祛斑技术介绍"
    db_session.commit()

    FaissService.rebuild(eid, TEST_TYPE)

    # 搜索新内容
    results = FaissService.search(eid, TEST_TYPE, "激光祛斑", top_k=5)
    assert len(results) >= 1, "rebuild 后应能搜到更新后的内容"

    # 清理数据库（测试结束后自动 drop_all，这里只删索引文件）
    db_session.rollback()
```

---

## 5. 不可破坏的约束

- ❌ 不依赖真实 embedding 模型（慢且需要本地文件）
- ❌ 不依赖 Celery/RabbitMQ/Redis（纯 Faiss 逻辑测试）
- ⚠️ 测试用的 eid=9999、8888 不会与真实数据冲突（测试是独立 SQLite 内存库）
- ⚠️ `_cleanup` fixture 确保每次测试结束后删除临时索引文件

---

## 6. 验收标准

- [ ] `cd backend && pytest tests/a_track/test_a4_faiss.py -v` → **3 passed**
- [ ] `cd backend && pytest tests/a_track/test_a0_industry_pack.py tests/a_track/test_a1_auth.py tests/a_track/test_a2_kb.py tests/a_track/test_a3_gateway.py tests/a_track/test_a4_faiss.py -v` → **29 passed**（不破坏已有）

---

## 7. 完成后

- [ ] 在 TODO-A.md 勾选 `A4-3`
- [ ] 在 `执行记录/A4.md` 追加 A4-3 执行记录（含 pytest 输出）

---

*Step A4-3 · 新建 test_a4_faiss.py*
# Step A3-4: 新建 `test_a3_gateway.py` — 6 条用例

> **所属任务**: A3 · LLM Gateway × 4 Adapter
> **前置**: [A3-3](./A3-3.md) 只读验证通过 | **后续**: 🔍 A3 验收（审查者）
> **类型**: 🧪 新建测试文件

---

## ⚠️ 执行前自检（必须全部 ✅ 才能动手）

- [ ] 我知道要新建 `backend/tests/a_track/test_a3_gateway.py`，写 6 条测试用例
- [ ] 我知道测试需要 mock HTTP 调用（用 `unittest.mock.patch` / `pytest.monkeypatch`），不依赖真实 API Key
- [ ] 我知道现有测试模式：走 `client` fixture + `db_session` fixture（conftest.py）
- [ ] 我能在改完后跑 `pytest tests/a_track/test_a3_gateway.py -v` 验证 6 passed

> **如果任一未勾选 → 不要动手，向用户提问直到全部能勾选。**

---

## 1. 目标

新建测试文件，用 mock 验证 gateway 搜索/cite解析/fallback/重试四块。

---

## 2. 涉改文件

| 文件 | 权限 | 说明 |
|------|:----:|------|
| `backend/tests/a_track/test_a3_gateway.py` | 🆕 | 6 条测试用例 |

---

## 3. 用例清单

### T-A3-01：`simple_search()` 构造 SearchRequest 正确

**测试内容**：
- 调 `simple_search("静安寺皮肤管理推荐")` 
- 断言 `query == "静安寺皮肤管理推荐"`、`timeout == 200`、`max_keywords == 3`、`limit == 10`

**mock**：不需要（纯数据构造，无 HTTP）

---

### T-A3-02：`DoubaoAdapter.search()` 正确解析 citations

**测试内容**：
- Mock `httpx.AsyncClient.post` 返回一个仿照 `test_doubao.py` 格式的 JSON（含 `output[0].content[0].text` + `annotations[type=url_citation]` 至少 2 条）
- 调 `adapter.search()` → 断言 `resp.ok == True`、`len(resp.citations) >= 2`、单条 citation 含 `url`/`title`/`summary`/`site_name`
- 断言 `resp.answer` 非空

**mock 结构参考**（给执行者）：
```python
MOCK_RESPONSE = {
    "id": "resp_test123",
    "model": "doubao-seed-2-1-pro-260628",
    "status": "completed",
    "output": [{
        "content": [{
            "text": "根据搜索结果，静安寺附近有多家皮肤管理门店...",
            "annotations": [
                {
                    "type": "url_citation",
                    "url": "https://www.dianping.com/shop/123",
                    "title": "XX皮肤管理中心 - 大众点评",
                    "summary": "这家店位于静安寺附近，主打韩式皮肤管理..." * 10,  # 模拟长摘要
                    "site_name": "大众点评",
                    "publish_time": "2026-06-01",
                },
                {
                    "type": "url_citation",
                    "url": "https://www.xiaohongshu.com/note/456",
                    "title": "静安寺皮肤管理探店笔记",
                    "summary": "上周去了静安寺的一家皮肤管理店..." * 10,
                    "site_name": "小红书",
                },
            ],
        }]
    }],
    "usage": {"input_tokens": 150, "output_tokens": 500, "total_tokens": 650},
}
```

---

### T-A3-03：`gateway.search()` — 豆包搜索成功时立即返回

**测试内容**：
- Mock `DoubaoAdapter.search()` 返回一个成功的 `SearchResponse`
- 调 `gateway.search(simple_search("测试查询", engine="doubao"))` 
- 断言 `resp.ok == True`、`resp.engine == "doubao"`

**mock 方式**：`pytest.monkeypatch` 或 `unittest.mock.patch` 替换 `DoubaoAdapter.search`

---

### T-A3-04：`gateway.search()` — 无 API Key 时走 chat fallback（simulated=true）

**测试内容**：
- 使用 `engine="kimi"`（假设没有 API Key，`adapter.api_key == ""`）
- Mock `_OpenAICompatAdapter.chat()` 返回一个模拟搜索的回答
- 调 `gateway.search(simple_search("测试查询", engine="kimi"))`
- 断言 `resp.ok == True`、`resp.extra["simulated"] == True`

**mock 方式**：patch `get_adapter` 返回的对象的 `api_key` 为空 + `chat()` 返回模拟回答

---

### T-A3-05：`gateway.search()` — 全部失败时返回 error

**测试内容**：
- Mock `get_adapter` 返回一个无 api_key 的 adapter（全部引擎不可用）
- 同时 mock `chat()` 也失败
- 调 `gateway.search(simple_search("测试"))`
- 断言 `resp.ok == False`、`resp.error` 非空

---

### T-A3-06：`simple_prompt()` + `gateway.chat()` — fallback 链验证

**测试内容**：
- Mock `DoubaoAdapter.chat()` 失败 → Mock `DeepseekAdapter.chat()` 成功
- 调 `chat(simple_prompt("你好", engine="doubao"))` 
- 断言 `resp.engine == "deepseek"`（降级成功）、`resp.retry_count > 0`

---

## 4. 代码骨架（给执行者参考）

测试文件的基础结构：

```python
"""A3 LLM Gateway 测试 — mock 搜索/cite解析/fallback/重试

用例对照 G-L3-开发者A任务手册.md §11.3：
  T-A3-01 ~ T-A3-06
"""
import pytest
from unittest.mock import AsyncMock, patch, MagicMock

from app.core.llm.schemas import (
    SearchRequest, SearchResponse, SearchCitation, LLMUsage,
    LLMRequest, LLMResponse, ChatMessage,
)
from app.core.llm import simple_search, simple_prompt, chat, search


# ── T-A3-01：simple_search() 构造正确 ──
def test_simple_search_constructs_correctly():
    req = simple_search("静安寺皮肤管理推荐")
    assert req.query == "静安寺皮肤管理推荐"
    assert req.timeout == 200
    assert req.max_keywords == 3
    assert req.limit == 10


# ── T-A3-02：DoubaoAdapter.search() 正确解析 citations ──
@pytest.mark.asyncio
async def test_doubao_search_parses_citations():
    from app.core.llm.adapters import DoubaoAdapter

    # Mock HTTP response
    mock_payload = {
        "id": "test123",
        "model": "doubao-seed-2-1-pro-260628",
        "output": [{
            "content": [{
                "text": "根据搜索结果，静安寺附近有多家皮肤管理门店...",
                "annotations": [
                    {
                        "type": "url_citation",
                        "url": "https://www.dianping.com/shop/123",
                        "title": "XX皮肤管理中心",
                        "summary": "这家店位于静安寺附近，主打韩式皮肤管理..." * 10,
                        "site_name": "大众点评",
                    },
                    {
                        "type": "url_citation",
                        "url": "https://www.xiaohongshu.com/note/456",
                        "title": "静安寺皮肤管理探店",
                        "summary": "上周去了静安寺的一家皮肤管理店..." * 10,
                        "site_name": "小红书",
                    },
                ],
            }]
        }],
        "usage": {"input_tokens": 150, "output_tokens": 500, "total_tokens": 650},
    }

    adapter = DoubaoAdapter()
    with patch("httpx.AsyncClient.post") as mock_post:
        mock_resp = AsyncMock()
        mock_resp.status_code = 200
        mock_resp.json.return_value = mock_payload
        mock_post.return_value = mock_resp

        resp = await adapter.search(SearchRequest(query="静安寺皮肤管理推荐"))

    assert resp.ok is True
    assert len(resp.citations) == 2
    assert resp.citations[0].url == "https://www.dianping.com/shop/123"
    assert resp.citations[0].site_name == "大众点评"
    assert len(resp.answer) > 0
    assert resp.usage.prompt_tokens == 150


# ── T-A3-03：gateway.search() — 豆包成功时立即返回 ──
@pytest.mark.asyncio
async def test_gateway_search_doubao_success():
    mock_resp = SearchResponse(
        engine="doubao",
        model="doubao-seed-2-1-pro-260628",
        answer="静安寺附近有多家皮肤管理门店...",
        citations=[SearchCitation(url="https://example.com", title="测试", summary="摘要", site_name="测试站")],
        usage=LLMUsage(prompt_tokens=100, completion_tokens=200, total_tokens=300),
    )

    with patch("app.core.llm.gateway.get_adapter") as mock_get:
        mock_adapter = MagicMock()
        mock_adapter.api_key = "fake-key"
        mock_adapter.search = AsyncMock(return_value=mock_resp)
        mock_get.return_value = mock_adapter

        resp = await search(simple_search("测试查询", engine="doubao"))

    assert resp.ok is True
    assert resp.engine == "doubao"
    assert "静安寺" in resp.answer


# ── T-A3-04：gateway.search() — 无 API Key 时走 chat fallback ──
@pytest.mark.asyncio
async def test_gateway_search_fallback_to_chat():
    mock_chat_resp = LLMResponse(
        engine="kimi",
        model="moonshot-v1-32k",
        content="## AI 回答\n模拟搜索回答内容...\n## 品牌提及\nXX皮肤管理",
        usage=LLMUsage(prompt_tokens=50, completion_tokens=300, total_tokens=350),
    )

    with patch("app.core.llm.gateway.get_adapter") as mock_get:
        mock_adapter = MagicMock()
        mock_adapter.api_key = ""  # 无 API Key → skip 联网搜索，走 chat
        mock_adapter.search = AsyncMock(return_value=SearchResponse(
            engine="kimi", model="", error="not configured"
        ))
        mock_adapter.chat = AsyncMock(return_value=mock_chat_resp)
        mock_get.return_value = mock_adapter

        resp = await search(simple_search("测试查询", engine="kimi"))

    assert resp.ok is True
    assert resp.extra.get("simulated") is True


# ── T-A3-05：gateway.search() — 全部失败时返回 error ──
@pytest.mark.asyncio
async def test_gateway_search_all_fail():
    with patch("app.core.llm.gateway.get_adapter") as mock_get:
        mock_adapter = MagicMock()
        mock_adapter.api_key = ""  # 无 API Key
        mock_adapter.search = AsyncMock(return_value=SearchResponse(
            engine="none", model="", error="not configured"
        ))
        mock_adapter.chat = AsyncMock(return_value=LLMResponse(
            engine="none", model="", content="", usage=LLMUsage(), error="chat failed"
        ))
        mock_get.return_value = mock_adapter

        resp = await search(simple_search("测试"))

    assert resp.ok is False
    assert resp.error is not None
    assert len(resp.error) > 0


# ── T-A3-06：gateway.chat() — fallback 链验证 ──
@pytest.mark.asyncio
async def test_gateway_chat_fallback():
    fail_resp = LLMResponse(
        engine="doubao", model="doubao-pro-32k", content="",
        usage=LLMUsage(), error="HTTP 500", retry_count=0,
    )
    success_resp = LLMResponse(
        engine="deepseek", model="deepseek-chat", content="你好！",
        usage=LLMUsage(prompt_tokens=10, completion_tokens=5, total_tokens=15),
        retry_count=1,
    )

    # Patch get_adapter: first call → doubao (fail), second → deepseek (success)
    call_count = [0]

    def mock_get_adapter(engine):
        mock = MagicMock()
        if engine == "doubao":
            mock.api_key = "fake-doubao-key"
            mock.chat = AsyncMock(return_value=fail_resp)
        else:
            mock.api_key = "fake-deepseek-key"
            mock.chat = AsyncMock(return_value=success_resp)
        return mock

    with patch("app.core.llm.gateway.get_adapter", side_effect=mock_get_adapter):
        resp = await chat(simple_prompt("你好", engine="doubao"))

    assert resp.ok is True
    assert resp.engine == "deepseek"
    assert resp.retry_count > 0
```

---

## 5. 不可破坏的约束

- ❌ 不依赖真实 API Key（全部 mock）
- ❌ 不修改任何被测代码文件
- ⚠️ 测试中的 `asyncio` 需要 `pytest-asyncio`（检查 `backend/requirements-dev.txt` 或 `pyproject.toml` 是否有此依赖；如果没有，用 `pip install pytest-asyncio` 安装）

---

## 6. 验收标准

- [ ] `cd backend && pip install pytest-asyncio`（如果未安装）
- [ ] `cd backend && pytest tests/a_track/test_a3_gateway.py -v` → **6 passed**
- [ ] `cd backend && pytest tests/a_track/test_a0_industry_pack.py tests/a_track/test_a1_auth.py tests/a_track/test_a3_gateway.py -v` → **18 passed**（不破坏已有测试）

---

## 7. 完成后

- [ ] 在 TODO-A.md 勾选 `A3-4`
- [ ] 在 `执行记录/A3.md` 追加 A3-4 执行记录（含 pytest 输出）

---

*Step A3-4 · 新建 test_a3_gateway.py*

# Step A2-2: 🔍 核实 kb_faqs / kb_signals / kb_externals + _BaseKBService 复用

> **所属任务**: A2 · KB CRUD（Fact/FAQ/Signal/External）
> **前置**: [A2-1](./A2-1.md) | **后续**: [A2-3](./A2-3.md)
> **类型**: 🔍 只读验证，不改代码

---

## ⚠️ 执行前自检（必须全部 ✅ 才能动手）

- [ ] 我知道这一步**不改代码** — 只读核对 FAQ/Signal/External 三个子模块
- [ ] 我知道检查重点：三个 Service 是否复用 `_BaseKBService` 而非重写逻辑；路由层 endpoint 是否完整；schema 字段与 model 是否对齐
- [ ] 我知道 _BaseKBService 已在 A2-1 验证过 → 本轮只验证「调用是否传了 enterprise_id」

> **如果任一未勾选 → 不要动手，向用户提问直到全部能勾选。**

---

## 1. 目标

依次核实 FAQ（6 端点）、Signal（5 端点）、External（5 端点）的路由/服务/schema/模型四层，确认全部正确复用 `_BaseKBService` 基类，无重复代码或逻辑缺陷。

---

## 2. 涉改文件

| 文件 | 权限 | 说明 |
|------|:----:|------|
| `backend/app/api/v1/user/kb.py` | 🔍 | 第 130-246 行，faq/signal/external 路由 |
| `backend/app/service/kb_service.py` | 🔍 | FaqService / SignalService / ExternalService（第 211-293 行） |
| `backend/app/schemas/kb.py` | 🔍 | KBFaq* / KBSignal* / KBExternal* schema |
| `backend/app/models/kb.py` | 🔍 | KBFaq / KBSignal / KBExternal 模型 |

---

## 3. 验证清单

### 3.1 FAQ（6 端点）

**路由**（`kb.py` 第 130-172 行）：

```
[ ] GET    /user/kb/faqs            → list_faqs      — KBFaqListParams 查询参数
[ ] POST   /user/kb/faqs            → create_faq     — @require_role(写角色)，status_code=201
[ ] GET    /user/kb/faqs/{faq_id}   → get_faq        — 不存在→404
[ ] PUT    /user/kb/faqs/{faq_id}   → update_faq     — @require_role
[ ] DELETE /user/kb/faqs/{faq_id}   → delete_faq     — @require_role
[ ] 无 bulk-delete（FAQ 没有批量删除，检查确认）
```

**服务**（`kb_service.py` FaqService 第 211-235 行）：

```
[ ] class FaqService(_BaseKBService):
    MODEL = KBFaq
    [ ] list() → cls._list(db, eid, params)          ← 复用基类，eid 传入
    [ ] get() → cls._get(db, eid, iid)               ← 复用基类
    [ ] create() → cls._create(db, eid, data)         ← 复用基类
    [ ] update() → cls._update(db, eid, iid, data)    ← 复用基类
    [ ] delete() → cls._delete(db, eid, iid)          ← 复用基类
```

**Schema**（`schemas/kb.py` KBFaqBase/Create/Update/Response/ListParams）：

```
[ ] KBFaqBase   — question(必填) / answer(必填) / category / tags / fact_refs / source / status
[ ] KBFaqCreate — 继承 Base，加 verified=False
[ ] KBFaqUpdate — 全部 Optional
[ ] KBFaqResponse — 继承 Base + ID + Timestamp + enterprise_id + verified + embedding_id
[ ] KBFaqListParams — 继承 PaginationParams + category / verified（无 source_type — FAQ 不用此字段）
```

**模型**（`models/kb.py` KBFaq 第 25-40 行）：

```
[ ] 表名 "kb_faqs"
[ ] 字段对齐：question(String500) / answer(Text) / category / tags(JSON) / fact_refs(JSON)
    / source(String30) / status(String20) / verified(Boolean) / embedding_id / metadata_(JSON)
[ ] TenantMixin（enterprise_id）+ TimestampMixin
```

### 3.2 Signal（5 端点）

**路由**（`kb.py` 第 175-209 行）：

```
[ ] GET    /user/kb/signals              → list_signals    — KBSignalListParams（signal_type/status）
[ ] POST   /user/kb/signals              → create_signal   — @require_role，status_code=201
[ ] PUT    /user/kb/signals/{signal_id}  → update_signal   — @require_role
[ ] DELETE /user/kb/signals/{signal_id}  → delete_signal   — @require_role
[ ] 无 GET /signals/{id}（Signal 没有单独详情端点 — 确认这是设计如此还是遗漏）
```

> ⚠️ **注意**：Signal 没有 `GET /signals/{id}` 单独详情端点。如果这是有意省略，执行记录中备注"设计如此"；如果感觉是遗漏，执行记录中标注建议加上。

**服务**（`kb_service.py` SignalService 第 239-264 行）：

```
[ ] class SignalService(_BaseKBService):
    MODEL = KBSignal
    [ ] list/create/update/delete 均走基类方法，eid 传入
    [ ] 无 get() 方法（与路由层无 GET /{id} 一致）
```

**Schema**（`schemas/kb.py` KBSignalBase/Create/Update/Response/ListParams）：

```
[ ] KBSignalBase    — signal_type(必填) / content(必填) / source / confidence(0-100) / fact_refs / status / metadata
[ ] KBSignalCreate  — 继承 Base，无额外字段
[ ] KBSignalUpdate  — 全部 Optional
[ ] KBSignalResponse — 继承 Base + ID + Timestamp + enterprise_id（无 verified/embedding_id，信号不需要这些）
[ ] KBSignalListParams — 继承 PaginationParams + signal_type / status
```

**模型**（`models/kb.py` KBSignal 第 43-55 行）：

```
[ ] 表名 "kb_signals"
[ ] 字段：signal_type(String50) / content(Text) / source(String200) / confidence(Integer默认0)
    / fact_refs(JSON) / status(String20默认"pending") / metadata_(JSON)
[ ] 无 verified / embedding_id 字段（与 Schema 一致，信号不需要向量化）
```

### 3.3 External（5 端点）

**路由**（`kb.py` 第 212-246 行）：

```
[ ] GET    /user/kb/externals          → list_externals   — KBExternalListParams（source_platform/status）
[ ] POST   /user/kb/externals          → create_external  — @require_role，status_code=201
[ ] PUT    /user/kb/externals/{ext_id} → update_external  — @require_role
[ ] DELETE /user/kb/externals/{ext_id} → delete_external  — @require_role
[ ] 无 GET /externals/{id}（同 Signal，没有单独详情端点）
```

**服务**（`kb_service.py` ExternalService 第 267-293 行）：

```
[ ] class ExternalService(_BaseKBService):
    MODEL = KBExternal
    [ ] list/create/update/delete 均走基类方法，eid 传入
    [ ] 无 get() 方法
```

**Schema**（`schemas/kb.py` KBExternalBase/Create/Update/Response/ListParams）：

```
[ ] KBExternalBase     — url / title / source_platform / content / summary / status / fact_refs / metadata
[ ] KBExternalCreate   — 继承 Base，无额外字段
[ ] KBExternalUpdate   — 全部 Optional
[ ] KBExternalResponse — 继承 Base + ID + Timestamp + enterprise_id + last_fetched_at
[ ] KBExternalListParams — 继承 PaginationParams + source_platform / status
```

**模型**（`models/kb.py` KBExternal 第 58-74 行）：

```
[ ] 表名 "kb_externals"
[ ] 字段：url(String1000) / title(String500) / source_platform(String50) / content(Text) / summary(Text)
    / status(String20) / last_fetched_at(DateTime) / fact_refs(JSON) / source_diagnosis_id(FK→source_diagnoses)
    / metadata_(JSON)
[ ] source_diagnosis_id 外键 → 可追溯到诊断结果（A7 用）
```

---

## 4. 验收标准

```
[ ] FAQ 6 端点全部 ✅（路由/服务/模型/Schema 四层一致）
[ ] Signal 5 端点全部 ✅（确认无 GET /{id} 是设计如此）
[ ] External 5 端点全部 ✅（确认 source_diagnosis_id 外键）
[ ] 三个 Service 均正确复用 _BaseKBService，无重写 _list/_get/_create/_update/_delete
[ ] 三个 Service 调用基类方法时均传入 enterprise_id（eid）
```

---

## 5. 完成后

- [ ] 在 TODO-A.md 勾选 `A2-2`
- [ ] 在 `执行记录/A2.md` 追加 A2-2 验证结果

---

*Step A2-2 · 只读验证 — FAQ/Signal/External 四层核对*

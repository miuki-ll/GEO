# Step A9-1: 入驻向导 — 更新 API 对接 + SSE 进度 + 分步收集表单数据

> **所属任务**: A9 · 前端后端对接
> **前置**: A5（开店向导 API）、A6（SSE）| **后续**: [A9-2](./A9-2.md)
> **类型**: 🛠️ 修改现有代码

---

## ⚠️ 执行前自检（必须全部 ✅ 才能动手）

- [ ] 我知道 A5 后端的 `OnboardingRunRequest` schema（`schemas/onboarding.py`）：enterprise/Brand/Store/Service/competitors/target_customers/raw_inputs/seed_facts/target_engines/search_enabled
- [ ] 我知道前端表单目前缺少 brand/services/competitors/target_customers/raw_inputs/target_engines 字段，需要在现有步骤中补齐
- [ ] 我知道 SSE 进度用 `GET /user/onboarding/events/{task_id}`（A6），替换现有轮询 `GET /status/{task_id}`
- [ ] 我知道"分步保存"=每步数据存 pinia store（已有 persist），最后一步组装完整请求体 POST /run

> **如果任一未勾选 → 不要动手，向用户提问直到全部能勾选。**

---

## 1. 目标

将入驻向导从"假数据演示"升级为"真实 API 对接"：收集完整表单 → POST /run → SSE 进度 → 完成后跳转。

---

## 2. 涉改文件

| 文件 | 权限 | 说明 |
|------|:----:|------|
| `frontend/src/api/onboarding.ts` | 🛠️ | 更新类型定义 + 新增 SSE 函数 |
| `frontend/src/stores/onboarding.ts` | 🛠️ | 扩展 persist 字段，存储完整表单数据 |
| `frontend/src/views/onboarding/Index.vue` | 🛠️ | 更新表单收集所有字段 + SSE 进度 + 提交 |

---

## 3. 改动清单

### 3.1 改动 1：`api/onboarding.ts` — 更新类型 + 加 SSE

**文件**：`frontend/src/api/onboarding.ts`

**改什么**：替换 OnboardingRunRequest 类型，新增 subscribeOnboardingEvents 函数

```typescript
import request from '@/utils/request'
import type { ApiResponse } from '@/types'

// ── A5 OnboardingRunRequest（对齐 backend/app/schemas/onboarding.py）──

export interface EnterpriseInfo {
  name: string
  industry?: string
  license_no?: string
  contact_name?: string
  contact_phone?: string
}

export interface BrandInfo {
  name: string
  differentiator?: string
  slogan?: string
}

export interface StoreInfo {
  name: string
  city?: string
  district?: string
  address?: string
  phone?: string
  business_hours?: string
  is_primary?: boolean
}

export interface ServiceInfo {
  name: string
  description?: string
  category?: string
  price_hint?: string
  duration_minutes?: number
}

export interface SeedFact {
  title: string
  content: string
}

export interface OnboardingRunRequest {
  enterprise: EnterpriseInfo
  brand?: BrandInfo
  stores: StoreInfo[]
  services: ServiceInfo[]
  competitors: string[]
  target_customers: string
  raw_inputs: string
  seed_facts: SeedFact[]
  target_engines: string[]
  search_enabled: boolean
}

export interface OnboardingTaskResponse {
  task_id: number
  status: string
  progress_pct: number
  progress_message: string
  step: string
  result?: any
}

// ── API 函数 ──

export function runOnboarding(data: OnboardingRunRequest) {
  return request.post<any, ApiResponse<OnboardingTaskResponse>>('/api/v1/user/onboarding/run', data)
}

/** 创建 SSE 连接监听入驻进度。返回 EventSource，调用方负责 close。 */
export function subscribeOnboardingEvents(taskId: number): EventSource {
  const base = import.meta.env.VITE_API_BASE_URL || ''
  const token = localStorage.getItem('geo-user')
    ? JSON.parse(localStorage.getItem('geo-user') || '{}').token || ''
    : ''
  // SSE 需要 token 鉴权，通过 query param 传递（FastAPI SSE 不支持 header）
  const url = `${base}/api/v1/user/onboarding/events/${taskId}?token=${encodeURIComponent(token)}`
  return new EventSource(url)
}
```

### 3.2 改动 2：`stores/onboarding.ts` — 扩展 persist 字段

**文件**：`frontend/src/stores/onboarding.ts`

**改什么**：在 persist.pick 中增加 formData 相关字段

```typescript
// persist.pick 改为：
persist: {
  key: 'geo-onboarding',
  pick: ['currentStep', 'formData', 'taskId', 'status', 'progress'],
},
```

同时在 state 中新增 `formData` 字段：

```typescript
interface OnboardingState {
  currentStep: number
  status: 'idle' | 'pending' | 'running' | 'done' | 'failed'
  taskId: number | null
  progress: number
  message: string
  // 表单数据（分步收集）
  formData: {
    enterprise: { name: string; industry: string; license_no: string; contact_name: string; contact_phone: string }
    brand: { name: string; differentiator: string; slogan: string }
    stores: Array<{ name: string; city: string; district: string; address: string; phone: string; business_hours: string }>
    services: Array<{ name: string; description: string; category: string; price_hint: string }>
    competitors: string[]
    target_customers: string
    raw_inputs: string
    seed_facts: Array<{ title: string; content: string }>
    target_engines: string[]
  }
}
```

### 3.3 改动 3：`views/onboarding/Index.vue` — 表单 + SSE + 提交

**文件**：`frontend/src/views/onboarding/Index.vue`

**改动要点**：

1. **Step 0（企业信息）**：加 brand 字段（品牌名+slogan）、target_engines 多选、门店信息
2. **Step 1（知识种子）**：已有 seedFacts，保持不变
3. **新增中间 step**：服务项目列表、竞品名称、目标客群描述、raw_inputs
4. **最后 step**：组装 `OnboardingRunRequest` → 调 `runOnboarding()` → 切 SSE 监听进度
5. **SSE 替换轮询**：删除 `pollStatus()`，改用 `subscribeOnboardingEvents()`

**核心逻辑改动**（非完整文件，只写关键函数）：

```typescript
// ── 提交入驻 ──

let es: EventSource | null = null

async function start() {
  // 1. 组装请求体
  const body: OnboardingRunRequest = {
    enterprise: {
      name: form.name,
      industry: form.industry,
      license_no: form.license,
      contact_name: form.contact_name,
      contact_phone: form.contact_phone,
    },
    brand: {
      name: brandForm.name || form.name,
      differentiator: brandForm.differentiator,
      slogan: brandForm.slogan,
    },
    stores: [{
      name: form.name,
      city: storeForm.city,
      district: storeForm.district,
      address: form.address,
      phone: form.contact_phone,
      business_hours: storeForm.business_hours,
      is_primary: true,
    }],
    services: serviceList.value.map(s => ({
      name: s.name,
      description: s.description,
      category: s.category,
      price_hint: s.price_hint,
    })),
    competitors: competitorList.value.filter(Boolean),
    target_customers: targetCustomers.value,
    raw_inputs: rawInputs.value,
    seed_facts: seedFacts.value.filter(f => f.title && f.content),
    target_engines: selectedEngines.value,
    search_enabled: true,
  }

  try {
    store.setStatus('running', { progress: 0, message: '正在提交入驻数据…' })
    const res = await runOnboarding(body)
    const taskId = res.data?.task_id
    if (!taskId) throw new Error('未返回 task_id')

    store.taskId = taskId

    // 2. 切 SSE 监听进度
    es = subscribeOnboardingEvents(taskId)
    es.onmessage = (event) => {
      try {
        const data = JSON.parse(event.data)
        store.setStatus(data.status, {
          progress: data.progress_pct,
          message: data.progress_message,
        })
        if (data.status === 'done') {
          es?.close()
          ElMessage.success('入驻完成！')
          setTimeout(() => router.push('/outcomes'), 1000)
        } else if (data.status === 'failed') {
          es?.close()
          ElMessage.error(data.progress_message || '入驻失败')
          store.setStatus('failed')
        }
      } catch {}
    }
    es.onerror = () => {
      es?.close()
      store.setStatus('failed', { message: 'SSE 连接中断' })
    }
  } catch (e: any) {
    store.setStatus('idle')
    ElMessage.error(e?.response?.data?.detail || '提交失败')
  }
}

// 组件卸载时关闭 SSE
import { onBeforeUnmount } from 'vue'
onBeforeUnmount(() => { es?.close() })
```

**表单字段补齐**（在现有 template 中新增的 section）：

```
Step 0 补充：
  - 品牌名称（brand.name）
  - 差异化描述（brand.differentiator）
  - 品牌 Slogan（brand.slogan）
  - 主攻 AI 引擎（target_engines 多选：豆包/DeepSeek/Kimi/文心）
  - 门店城市/商圈（store.city / store.district）
  - 营业时间（store.business_hours）

Step 1（知识种子）之后新增 Step：
  - 服务项目列表（可增减，每项含 name/description/category/price_hint）
  - 已知竞品名称（textarea，一行一个）
  - 目标客群描述（textarea）
  - 自由输入（raw_inputs textarea）：用户想对 AI 说的任何话
```

> ⚠️ 注意：只改 API 对接和数据收集，UI 布局尽量不动。新增的表单字段放在对应的已有 step 中。

---

## 4. 不可破坏的约束

- ❌ 不删现有 UI 步骤结构
- ❌ 不改 `ProgressChain.vue` 组件
- ⚠️ SSE 需要 token 鉴权。如果后端 SSE 端点不支持 query param token，需要在 `core/sse.py` 或路由层加验证逻辑。**可以先查后端 A6 的 SSE 端点怎么验证用户的，前端对齐**
- ⚠️ 组件卸载时必须 `es.close()`，避免内存泄漏

---

## 5. 验收标准

- [ ] `cd frontend && npx vue-tsc --noEmit` → 无新增类型错误（onboarding 相关）
- [ ] 手动验证：打开入驻页 → 填写各步表单 → 点击开始 → SSE 进度条更新 → 100% 跳转 /outcomes

---

## 6. 完成后

- [ ] 在 TODO-A.md 勾选 `A9-1`
- [ ] 在 `执行记录/A9.md` 追加 A9-1 执行记录

---

*Step A9-1 · 入驻向导 API 对接*

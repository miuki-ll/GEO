# Step A6-1: Redis pub/sub SSE — helper + `/events` 端点 + nodes/runner 发布事件

> **所属任务**: A6 · Onboarding SSE 进度推送
> **前置**: [A5-1](./A5-1.md)（nodes.py 需先完成）| **后续**: [A6-2](./A6-2.md)
> **类型**: 🆕 新建 + 🛠️ 修改

---

## ⚠️ 执行前自检（必须全部 ✅ 才能动手）

- [ ] 我知道要新建 `backend/app/core/sse.py`（Redis pub/sub 封装），改 `onboarding.py`（加 SSE 端点），改 `nodes.py`（加 publish 调用），改 `runner.py`（加 publish 调用）
- [ ] 我知道 Redis 连接信息：密码 `123456`，host `localhost:6379`，Celery 用 db 0/1，SSE 用 db 3
- [ ] 我知道 SSE 格式：`data: {"progress_pct": 25, "progress_message": "...", "step": "PAIN", "status": "running"}\n\n`
- [ ] 我知道 `app/api/v1/user/onboarding.py` 的 SSE 端点不需要 JWT 中间件校验 WebSocket 升级 — SSE 走标准 HTTP，可以带 `?token=` 或沿用 Header 鉴权

> **如果任一未勾选 → 不要动手，向用户提问直到全部能勾选。**

---

## 1. 目标

将入驻进度从"前端轮询 HTTP JSON"升级为"SSE 实时推送"，基于 Redis pub/sub 解耦 node 发布与 SSE 推送。

---

## 2. 涉改文件

| 文件 | 权限 | 说明 |
|------|:----:|------|
| `backend/app/core/sse.py` | 🆕 | Redis pub/sub 封装（publish / subscribe generator） |
| `backend/app/api/v1/user/onboarding.py` | 🛠️ | 新增 `GET /onboarding/events/{task_id}` SSE 端点 |
| `backend/app/agents/graphs/onboarding/nodes.py` | 🛠️ | 每个节点完成后 publish 进度事件 |
| `backend/app/agents/runner.py` | 🛠️ | graph 开始时 publish "running"；完成/失败时 publish "completed"/"failed" |

---

## 3. 改动清单

### 3.1 改动 1：`core/sse.py`（🆕 新建）

**文件**：`backend/app/core/sse.py`

**改什么**：Redis pub/sub 封装，channel 统一走 `onboarding:{task_id}`

```python
"""Redis pub/sub — SSE 进度事件推送。

channel 约定：onboarding:{task_id}
消息体：JSON 字符串，包含 progress_pct / progress_message / step / status / next_route
"""
import json
import asyncio
from typing import AsyncGenerator, Dict, Any, Optional

from app.core.config import settings
from app.core.logging_config import get_logger

logger = get_logger(__name__)

# SSE pub/sub 使用独立的 Redis db（3），避免与 Celery broker(0)/backend(1)/result(2) 冲突
_SSE_REDIS_DB = 3


def _get_redis_url(db: int) -> str:
    """从 settings.REDIS_URL 替换 db 号。"""
    url = settings.REDIS_URL
    # redis://:password@host:port/db  → 替换 db 号
    if url.rstrip("/").endswith(f"/{db}"):
        return url
    # 去掉旧 db 号，拼新的
    base = url.rsplit("/", 1)[0]
    return f"{base}/{db}"


def channel_name(task_id: int) -> str:
    return f"onboarding:{task_id}"


def publish_progress(task_id: int, payload: Dict[str, Any]) -> None:
    """同步发布进度事件（供 nodes.py / runner.py 在 async 上下文外调用）。

    使用同步 redis 客户端，避免阻塞事件循环。
    """
    try:
        import redis
        r = redis.Redis.from_url(_get_redis_url(_SSE_REDIS_DB))
        msg = json.dumps(payload, ensure_ascii=False)
        r.publish(channel_name(task_id), msg)
        r.close()
    except Exception as e:
        logger.warning("[sse] publish failed task=%s: %s", task_id, e)


async def publish_progress_async(task_id: int, payload: Dict[str, Any]) -> None:
    """异步发布进度事件（供 async 函数使用）。"""
    try:
        import redis.asyncio as aioredis
        r = aioredis.Redis.from_url(_get_redis_url(_SSE_REDIS_DB))
        await r.publish(channel_name(task_id), json.dumps(payload, ensure_ascii=False))
        await r.close()
    except Exception as e:
        logger.warning("[sse] publish async failed task=%s: %s", task_id, e)


async def subscribe_progress(task_id: int) -> AsyncGenerator[str, None]:
    """订阅进度事件 → SSE 事件流生成器。

    用法（FastAPI StreamingResponse）：
        async def events():
            async for event in subscribe_progress(task_id):
                yield event
        return StreamingResponse(events(), media_type="text/event-stream")
    """
    try:
        import redis.asyncio as aioredis
        r = aioredis.Redis.from_url(_get_redis_url(_SSE_REDIS_DB))
        pubsub = r.pubsub()
        await pubsub.subscribe(channel_name(task_id))
        try:
            while True:
                message = await pubsub.get_message(ignore_subscribe_messages=True, timeout=30.0)
                if message and message.get("type") == "message":
                    data = message["data"]
                    if isinstance(data, bytes):
                        data = data.decode("utf-8")
                    yield f"data: {data}\n\n"
                    # 如果收到 completed 或 failed，结束推送
                    try:
                        payload = json.loads(data)
                        if payload.get("status") in ("completed", "failed"):
                            break
                    except Exception:
                        pass
        finally:
            await pubsub.unsubscribe(channel_name(task_id))
            await r.close()
    except Exception as e:
        logger.warning("[sse] subscribe failed task=%s: %s", task_id, e)
        yield f"data: {json.dumps({'error': str(e)})}\n\n"


# 快捷构造
def build_event(
    progress_pct: int,
    progress_message: str,
    step: str = "",
    status: str = "running",
    next_route: Optional[str] = None,
) -> Dict[str, Any]:
    payload = {
        "progress_pct": progress_pct,
        "progress_message": progress_message,
        "step": step,
        "status": status,
    }
    if next_route:
        payload["next_route"] = next_route
    return payload
```

### 3.2 改动 2：`onboarding.py` — 新增 SSE 端点

**文件**：`backend/app/api/v1/user/onboarding.py`

**位置**：`GET /status/{task_id}` 之后

**改什么**：新增 `GET /onboarding/events/{task_id}`，返回 `text/event-stream`

```python
from fastapi.responses import StreamingResponse

@router.get("/events/{task_id}", summary="SSE 进度推送")
async def onboarding_events(
    task_id: int,
    user: User = Depends(get_current_active_user),
    db: Session = Depends(get_db),
):
    """SSE 端点 — 实时推送入驻进度。

    前端用 EventSource 连接，无需轮询。
    """
    from app.core.sse import subscribe_progress

    # 确认任务存在且属于当前用户
    t = AgentTaskService.get(db, user.enterprise_id, task_id)
    if not t:
        raise HTTPException(status_code=404, detail="任务不存在")

    return StreamingResponse(
        subscribe_progress(task_id),
        media_type="text/event-stream",
        headers={
            "Cache-Control": "no-cache",
            "Connection": "keep-alive",
            "X-Accel-Buffering": "no",
        },
    )
```

### 3.3 改动 3：`nodes.py` — 每个节点 publish 进度

**文件**：`backend/app/agents/graphs/onboarding/nodes.py`

**位置**：每个节点函数内 `state["progress_pct"] = ...` 之后

**改什么**：加一行 `publish_progress()` 调用。在 `run_onboarding_nodes()` 入口也加一条。

**关键位置**（A5-1 的 `nodes.py` 骨架中有明确的 `STEP_PCT` 赋值点，每个赋值后加一行）：

```python
from app.core.sse import publish_progress, build_event

# 在 run_onboarding_nodes 开头：
if state.get("task_id"):
    publish_progress(state["task_id"], build_event(
        progress_pct=STEP_PCT[0], progress_message=STEP_MSG[0], step="DIAGNOSE",
    ))

# 在 diagnose_node 末尾（state["progress_pct"] = STEP_PCT[1] 之后）：
if state.get("task_id"):
    publish_progress(state["task_id"], build_event(
        progress_pct=STEP_PCT[1], progress_message=STEP_MSG[1], step="PAIN",
    ))

# pain_node 末尾：
if state.get("task_id"):
    publish_progress(state["task_id"], build_event(
        progress_pct=STEP_PCT[2], progress_message=STEP_MSG[2], step="PERSONA",
    ))

# persona_node 末尾：
if state.get("task_id"):
    publish_progress(state["task_id"], build_event(
        progress_pct=STEP_PCT[3], progress_message=STEP_MSG[3], step="COMPETITOR",
    ))

# competitor_node 末尾（done）：
if state.get("task_id"):
    publish_progress(state["task_id"], build_event(
        progress_pct=STEP_PCT["done"],
        progress_message="入驻分析完成",
        step="done",
        status="completed",
        next_route="/strategy-pack?draft=1",
    ))
```

### 3.4 改动 4：`runner.py` — 开始/异常时 publish

**文件**：`backend/app/agents/runner.py`

**位置**：`run_graph()` 函数内

**改什么**：开始执行时 publish "starting"；异常时 publish "failed"

```python
from app.core.sse import publish_progress_async, build_event

# 在 state 初始化之后、runner(state) 之前：
if task_id:
    await publish_progress_async(task_id, build_event(
        progress_pct=0, progress_message="starting", step="start", status="running",
    ))

# 在 except 块中（raise 之前）：
if task_id:
    await publish_progress_async(task_id, build_event(
        progress_pct=100, progress_message=str(e)[:200], step="error",
        status="failed",
    ))
```

---

## 4. 不可破坏的约束

- ❌ 不改 `AgentTaskService`（DB 的 progress 记录仍然写，SSE 是额外的推送通道）
- ❌ 不删除现有 `GET /status/{task_id}` 轮询接口（向后兼容，前端过渡期两套并存）
- ⚠️ Redis 不可用时：publish 静默失败（log warning），不阻塞业务。SSE 连接建立时会报错。
- ⚠️ SSE 连接超时：`subscribe_progress` 30 秒无消息 → 断开，前端 EventSource 自动重连即可

---

## 5. 验收标准

- [ ] `cd backend && python -c "from app.core.sse import publish_progress, build_event, channel_name; print('sse import OK')"` → 无错误
- [ ] `cd backend && python -c "from app.core.sse import build_event; e = build_event(25, '分析中…', step='PAIN'); print(e)"` → 输出 JSON 含 progress_pct=25
- [ ] `cd backend && python -c "
from app.core.sse import publish_progress, build_event
publish_progress(999, build_event(50, '测试消息', step='test'))
print('publish OK — 无异常')
"` → 输出 `publish OK — 无异常`（Redis 需运行）
- [ ] 验证 nodes.py 和 runner.py 的 import 路径正确：
  ```bash
  cd backend && python -c "from app.agents.graphs.onboarding.nodes import run_onboarding_nodes; print('nodes OK')"
  cd backend && python -c "from app.agents.runner import run_graph; print('runner OK')"
  ```

---

## 6. 完成后

- [ ] 在 TODO-A.md 勾选 `A6-1`
- [ ] 在 `执行记录/A6.md` 追加 A6-1 执行记录

---

*Step A6-1 · Redis pub/sub SSE — helper + 端点 + nodes/runner 发布事件*
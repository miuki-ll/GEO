# A1 执行记录 · 注册/登录/JWT/RBAC/租户隔离

> **执行者**：Cursor（开发者 A）  
> **状态**：A1-1～A1-4 全部完成，**待审查者验收**  
> **进度表**：`G-L3-开发进度.md` 中 A1 仍为 `doing`（审查通过后再改 `done` + NOTIFY B）  
> **TODO-A**：A1-1～A1-4 已勾选；无 A1-5（已改为审查者流程）

---

## A1-1 · 2026-07-17 15:25

- **改了什么**：
  - `backend/app/api/v1/auth.py` — `/login` 改为接收 `UserLogin` JSON；删除 `/login/json-login`；删除 `OAuth2PasswordRequestForm` import
  - `backend/app/models/auth.py` — `Enterprise.name` 加 `unique=True, index=True`
  - `backend/app/service/auth_service.py` — `EnterpriseService.create()` 开头查重，重复抛 `ValueError("该企业名称已被注册")`
  - `backend/alembic/versions/b2e8f4c91d03_add_unique_enterprise_name.py` — 🆕 unique index migration（已 `alembic upgrade head`）
- **验收结果**：
  - JSON `/login` → 200 + TokenPayload
  - `/login/json-login` → 404
  - 错误密码 → 401
  - 重复企业名 / 邮箱 → 400
  - `pytest tests/a_track/test_a0_industry_pack.py` → 6 passed（不受影响）
- **遇到问题**：无
- **越权自检**：只改 step §2 所列文件 + migration（step 要求）

---

## A1-2 · 2026-07-17 15:29

- **改了什么**：
  - `frontend/src/views/Login.vue` — 删 MVP `<el-alert>`；清空 demo 默认值；`handleLogin` → `userStore.login()`；错误提示
  - `frontend/src/views/Register.vue` — 表单校验 + `register()` → `userStore.login()` → `/onboarding`
- **验收结果**：
  - Vite proxy `/api/v1/auth/register` + `/login` 通
  - 错误密码 → 401；重复邮箱 → 400
  - `/login` `/register` 页面 200；源码无 MVP/demo 默认值
- **遇到问题 / 相对 step 的适配**（请审查者关注）：
  - `utils/request.ts` 对 HTTP 401 会 `reject(new Error('Unauthorized'))`，不一定保留 `e.response`
  - Login catch 因此同时判断 `e?.response?.status === 401 || e?.message === 'Unauthorized'`，否则错误密码会误显示「网络错误」
  - **未改** `request.ts` / `stores/user.ts` / `api/auth.ts`（符合 step §4 不涉及文件）
- **越权自检**：只改 Login.vue / Register.vue

---

## A1-3 · 2026-07-17 15:33

- **改了什么**：无（🔍 只读验证）
- **验收结果**（冒烟 12/12 PASS）：
  - register TokenPayload + role=owner
  - 重复邮箱/企业名 → 400
  - JSON login；错误密码 401；`/me`（无 hashed_password）；`/refresh`
  - 无 token → 401
  - 跨租户：B 读 A 的 KB fact → 404；B profile ≠ A enterprise_id
  - `PUT/GET /user/enterprise/profile` → `settings.target_engines == ["doubao","deepseek"]`
- **代码核对**：
  - JWT 链：`oauth2_scheme(auto_error=False)` → `get_current_user_or_none` → `get_current_user` → `require_role`
  - `kb_service` 查询均带 `enterprise_id` 过滤，**未发现遗漏**
  - 用户端路由均 `Depends(get_current_user)` 或别名 `get_current_active_user`
- **遇到问题**：无

---

## A1-4 · 2026-07-17 15:35

- **改了什么**：
  - `backend/tests/a_track/test_a1_auth.py` — 🆕 T-A1-01～06 + fixtures
  - `backend/tests/conftest.py` — 加共享 `client` fixture（TestClient）
- **验收结果**：
  ```
  pytest tests/a_track/test_a1_auth.py -v
  → 6 passed
  ```
  - 复跑（交接前）：`test_a1_auth.py` + `test_a0_industry_pack.py` → **12 passed**（2026-07-17 15:54）
- **相对 step 模板的适配**（请审查者关注）：
  1. 创建 fact API 声明 `status_code=201` → 断言改为 `in (200, 201)`
  2. `ListResponse` 的 `items` 在**顶层**（非 `data.items`）→ 兼容读取
- **遇到问题**：无；未发现 service 层漏 `enterprise_id`

---

## 涉改文件清单（供 git diff 对照）

| 文件 | Step | 权限 |
|------|------|------|
| `backend/app/api/v1/auth.py` | A1-1 | 🛠️ |
| `backend/app/models/auth.py` | A1-1 | 🛠️ |
| `backend/app/service/auth_service.py` | A1-1 | 🛠️ |
| `backend/alembic/versions/b2e8f4c91d03_add_unique_enterprise_name.py` | A1-1 | 🆕 |
| `frontend/src/views/Login.vue` | A1-2 | 🛠️ |
| `frontend/src/views/Register.vue` | A1-2 | 🛠️ |
| `backend/tests/a_track/test_a1_auth.py` | A1-4 | 🆕 |
| `backend/tests/conftest.py` | A1-4 | 🛠️ |
| `docs/开发手册/开发进度/TODO-A.md` | 勾选 | 🛠️ |

**不应被改（🔍）**：`security.py`、`schemas/auth.py`、`stores/user.ts`、`api/auth.ts`、`utils/request.ts`、`enterprise.py`、`kb.py`、`kb_service.py`

---

## 给审查者的命令

```bash
cd backend
pytest tests/a_track/test_a1_auth.py -v
# 期望：6 passed

# 对照 diff（仅 A1 范围）
git diff HEAD -- backend/app/api/v1/auth.py backend/app/models/auth.py backend/app/service/auth_service.py frontend/src/views/Login.vue frontend/src/views/Register.vue backend/tests/conftest.py
# 另看未跟踪：
#   backend/alembic/versions/b2e8f4c91d03_add_unique_enterprise_name.py
#   backend/tests/a_track/test_a1_auth.py
```

---

## 审查区（Claude 追加）

> **审查者**：Claude（规划者角色）  
> **审查日期**：2026-07-17  
> **审查结论**：✅ **全部通过**

---

### 审查者自检 3 条

| # | 检查项 | 结果 |
|---|--------|:--:|
| 1 | **直觉对不对** — 抛开 step，改动合理吗 | ✅ Login 合并为 JSON only 合理；Enterprise.name unique 正确；前端切真实 API + 表单校验干净，无多余抽象 |
| 2 | **实际跑通没** — 亲自跑验收命令 | ✅ `pytest tests/a_track/test_a1_auth.py -v` → **6 passed in 1.82s**（register/login/401/租户隔离/target_engines/重名拒绝，覆盖完整） |
| 3 | **越权没** — git diff 文件 == step §2 文件 | ✅ A1 涉改 8 个文件全部在清单内。LLM docstring 注释（`core/llm/*` 5 文件）属预存脏文件（初始 git status 已标 M），非 A1 所为，不扣分 |

---

### 审出问题

无阻断性问题。仅一个提醒：

- **预存脏文件**：`backend/app/core/llm/*`（5 文件，docstring 注释）、`.cursor/rules/` 删除、`handoff_a_to_b/*.json`、`frontend/package*.json` 在工作区有未提交变更。这些不是 A1 引入的，但建议在 A2 开始前清理（提交或还原），避免干扰后续 diff 审查。

---

### 处置

- [x] A1 审查通过
- [x] 更新 `G-L3-开发进度.md`：A1 `doing` → `done`，备注 `tests: A1 pass · 已通知 B → G1`
- [x] 更新统计：A done 1→2
- [x] **NOTIFY B**：A1 完成，G1（信封+JWT）可验。B 侧带 JWT 调 `GET /user/enterprise/profile` 应 200，无 JWT 应 401

> 审查结束。A1 done ✅

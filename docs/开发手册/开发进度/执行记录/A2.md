# A2 执行记录 · KB CRUD（Fact/FAQ/Signal/External）

> **执行者**：Cursor（开发者 A）  
> **状态**：A2-1～A2-4 全部完成，**待审查者验收**  
> **TODO-A**：A2-1～A2-4 已勾选；🔍 A2 验收由审查者执行

---

## A2-1 · 2026-07-19 12:43

- **改了什么**：无（🔍 只读验证）
- **验收结果**：3.1～3.4 全部 ✅

### 3.1 路由层（`kb.py` 66-128）

| 端点 | 结果 | 依据 |
|------|:----:|------|
| GET `/facts` → list_facts | ✅ | L67-79：`KBFactListParams` + `FactService.list` |
| POST `/facts` → create_fact | ✅ | L82-87：`KBFactCreate` + `@require_role(_WRITE_ROLES)` |
| GET `/facts/{id}` → get_fact | ✅ | L90-95：`FactService.get` + 不存在 → 404 |
| PUT `/facts/{id}` → update_fact | ✅ | L98-105：`KBFactUpdate` + `@require_role` + ValueError→404 |
| DELETE `/facts/{id}` → delete_fact | ✅ | L108-115：`@require_role` + ValueError→404 |
| POST `/facts/bulk-delete` | ✅ | L118-127：`@require_role` + 循环 `FactService.delete` |
| 未登录 401 | ✅ | Router L39 `dependencies=[Depends(get_current_user)]` + 各端点再 Depends |

### 3.2 服务层（`kb_service.py`）

| 方法 | 结果 | 依据 |
|------|:----:|------|
| `_list` 租户过滤 | ✅ | L46：`filter(enterprise_id == enterprise_id)` |
| `_get` AND 双条件 | ✅ | L85：`id == item_id, enterprise_id == enterprise_id` |
| `_create` 强制注入 | ✅ | L97：`payload["enterprise_id"] = enterprise_id` |
| `_update` 先 _get | ✅ | L125-127：不存在抛 `ValueError("记录不存在")` |
| `_delete` 先 _get | ✅ | L146-149：同上 |
| FactService 绑定 | ✅ | L183-185：`MODEL=KBFact` / `RESP=KBFactResponse` |
| 分页/排序 | ✅ | L74-76：`order_by(desc(updated_at))` + offset/limit |
| keyword 模糊 | ✅ | L59-71：title/question/content ilike OR + getattr 安全 |

### 3.3 Schema 层（`schemas/kb.py`）

| Schema | 结果 | 依据 |
|--------|:----:|------|
| KBFactCreate | ✅ | L20-36：title(1-500)/content(≥1)/source_type 默认 manual/tags=[]/verified=False |
| KBFactUpdate | ✅ | L39-52：全部 Optional |
| KBFactResponse | ✅ | L55-58：Base + IDModel + TimestampResponse + enterprise_id + verified + embedding_id |
| KBFactListParams | ✅ | L61-64：继承 PaginationParams（page/page_size/keyword）+ category/verified/source_type |

### 3.4 模型层（`models/kb.py`）

| 项 | 结果 | 依据 |
|----|:----:|------|
| 表名 / Mixin | ✅ | L8-9：`kb_facts` + TenantMixin + TimestampMixin |
| 列类型/默认值 | ✅ | L11-20：与 schema 对齐（title String(500)/content Text/source_type 默认 manual/verified 默认 False 等） |

### 低严重度备注（不阻断）

- `bulk_delete_facts` L127：成功消息用 `len(data.ids)`，部分 id 因 ValueError 跳过时数字可能偏大。行为符合 step「循环 delete + 跳过不存在」，仅文案不准。

- **遇到问题**：无

---

## A2-2 · 2026-07-19 12:44

- **改了什么**：无（🔍 只读验证）
- **验收结果**：FAQ / Signal / External 四层全部 ✅；三个 Service 均复用 `_BaseKBService` 且传入 eid

### 3.1 FAQ（6 端点）

| 层 | 结果 | 依据 |
|----|:----:|------|
| 路由 | ✅ | `kb.py` L130-172：list/create(201)/get(404)/update/delete + `@require_role`；确认无 bulk-delete |
| 服务 | ✅ | `FaqService(_BaseKBService)` L211-236：MODEL=KBFaq；list/get/create/update/delete 均 `cls._*(db, eid, …)` |
| Schema | ✅ | question/answer 必填；Create+verified；Update 全 Optional；Response+enterprise_id/verified/embedding_id；ListParams=category/verified（无 source_type） |
| 模型 | ✅ | `kb_faqs` + TenantMixin + TimestampMixin；字段与 schema 对齐 |

### 3.2 Signal（5 端点）

| 层 | 结果 | 依据 |
|----|:----:|------|
| 路由 | ✅ | `kb.py` L175-209：list/create(201)/update/delete；**无 GET /signals/{id}** |
| 服务 | ✅ | `SignalService(_BaseKBService)` L239-264：MODEL=KBSignal；list/create/update/delete 走基类+eid |
| Schema | ✅ | signal_type/content 必填；confidence 0-100；Response 无 verified/embedding_id；ListParams=signal_type/status |
| 模型 | ✅ | `kb_signals`；无 verified/embedding_id（与 schema 一致） |

**无 GET /{id} 判定**：设计如此（Signal 以列表+筛选为主，非单条编辑流）。Service 层仍有 `get()`（L247-250，与 Fact/FAQ 对称，可供内部调用），路由未暴露 — 非遗漏。

### 3.3 External（5 端点）

| 层 | 结果 | 依据 |
|----|:----:|------|
| 路由 | ✅ | `kb.py` L212-246：list/create(201)/update/delete；**无 GET /externals/{id}** |
| 服务 | ✅ | `ExternalService(_BaseKBService)` L267-292：MODEL=KBExternal；list/create/update/delete 走基类+eid |
| Schema | ✅ | url/title/…/fact_refs/metadata；Response+last_fetched_at；ListParams=source_platform/status |
| 模型 | ✅ | `kb_externals`；含 `source_diagnosis_id` FK→`source_diagnoses`（A7 追溯用） |

**无 GET /{id} 判定**：同 Signal，设计如此。Service 层有 `get()`（L276-278），路由未暴露。

### 复用确认

- **遇到问题**：无

---

## A2-3 · 2026-07-19 12:45

- **改了什么**：无（🔍 只读验证）
- **验收结果**：Summary / Freshness / thin_kb / touch_kb 全部 ✅，无跨租户漏洞

### 3.1 KB Summary

| 项 | 结果 | 依据 |
|----|:----:|------|
| 路由 GET `/summary` | ✅ | `kb.py` L50-52：`get_current_user` → `ResponseModel[KBSummary]` |
| 四表 + verified 计数 | ✅ | `KBSummaryService.summary` L298-313：均 `filter(enterprise_id)` |
| last_updated | ✅ | L302-306：取 `Enterprise.kb_updated_at` |

### 3.2 KB Freshness

| 项 | 结果 | 依据 |
|----|:----:|------|
| 路由 GET `/freshness` | ✅ | `kb.py` L55-63：返回 `{freshness, thin_kb}` |
| kb_freshness() | ✅ | L12-20：按 `Enterprise.id` 取 `kb_updated_at`；返回 ISO / `is_stale` / message |

> 备注：`is_stale` 仅在 `kb_updated_at is None` 时为 True（未更新过），非按天数老化 — 与当前实现一致。

### 3.3 thin KB Check

| 项 | 结果 | 依据 |
|----|:----:|------|
| service_count | ✅ | L25-28：`Service.enterprise_id` + `status=="active"` |
| faq_count / verified_facts | ✅ | L29-33：均带 enterprise_id |
| passed 门槛 | ✅ | L34：`>=2 services and >=3 faqs` |
| 返回字段 | ✅ | L35-41：passed/counts/requirements/message 齐全 |
| Service 模型 | ✅ | `models/auth.py` L123-135：表 `services`，`status` 默认 `"active"` |

**引用情况**：当前被 `GET /user/kb/freshness` + MCP `kb_freshness_tool` 调用；A5/B1 业务接线在后续 step（A5-5 等），门槛逻辑本身符合设计。

- **遇到问题**：无

---

## A2-4 · 2026-07-19 12:48

- **改了什么**：
  - 🆕 `backend/tests/a_track/test_a2_kb.py` — 8 条用例（T-A2-01～08）+ `kb_user` / `two_kb_tenants` fixtures
- **验收结果**：
  - `pytest tests/a_track/test_a2_kb.py -v` → **8 passed**
  - `pytest tests/a_track/test_a0_industry_pack.py tests/a_track/test_a1_auth.py tests/a_track/test_a2_kb.py -v` → **20 passed**（A0×6 + A1×6 + A2×8；step 写「14+8=22」为算术笔误）
- **遇到问题**：无（Windows 需用 `backend\.venv\Scripts\python.exe -m pytest`）
- **越权自检**：只新建 step §2 所列测试文件，未改路由/service/schema/模型

---

## 🔍 A2 验收（审查者）· 2026-07-19

### 审查者自检 3 条

- [x] **直觉对不对** — ✅ 改动合理。
  - A2-1~A2-3：只读验证，四层核对完整，执行记录详细标注了每项依据
  - A2-4：测试用例设计合理（6 API + 2 Service），覆盖 CRUD/分页/租户隔离/Summary/Freshness/关键词搜索
  - 低严重度备注（bulk-delete 计数不准、Signal/External 无 GET /{id}）已标明，不阻断
- [x] **实际跑通没** — ✅ `pytest tests/a_track/test_a0_industry_pack.py tests/a_track/test_a1_auth.py tests/a_track/test_a2_kb.py tests/a_track/test_a3_gateway.py -v` → **26 passed**
- [x] **越权没** — ✅ A2-1~A2-3 🔍 只读未改代码；A2-4 仅新建 `test_a2_kb.py`（step §2 唯一涉改文件），无越权改动

### 审查结论：**全部通过 ✅**

A2（KB CRUD）验收通过。8 条测试全 PASS，无越权改动，无破坏已有测试。

下一步：更新进度表 A2 → `done`，NOTIFY B → fact_refs 可追溯。

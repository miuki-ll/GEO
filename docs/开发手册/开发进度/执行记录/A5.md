# A5 执行记录 · 开店向导 API

## A5-1 · 2026-07-19 16:10

- **改了什么**：
  - `backend/app/agents/graphs/onboarding/nodes.py` — 空壳重写为 4 个 LLM 节点（`diagnose_node` / `pain_node` / `persona_node` / `competitor_node`）+ `run_onboarding_nodes` 编排；DIAGNOSE 支持并行 `gateway.search()`（`search_enabled=False` 可跳过）；JSON 解析失败有兜底
  - `state.py` — 按 step 说明不改（TypedDict total=False 可直接存新字段）
- **验收结果**：
  1. `python -c "from app.agents.graphs.onboarding.nodes import ...; print('import OK')"` → `import OK`
  2. `asyncio.run(run_onboarding_nodes(state))`（`search_enabled=False`）→
     ```
     step: done
     probes count: 3
     pain_points count: 0
     persona keys: ['age_range', 'genders', 'cities', 'core_needs', 'decision_factors', 'typical_queries', 'extra']
     competitors count: 0
     OK
     ```
- **遇到问题**：
  - 环境默认 chat 模型 `doubao-pro-32k` 返回 HTTP 404（`InvalidEndpointOrModel.NotFound`）；DeepSeek Key 为空，降级到豆包后失败
  - 因此 DIAGNOSE 走模板探针（3 条）、PERSONA 走 IndustryPack `default_persona`；PAIN/COMPETITOR 因 chat 失败为空列表
  - **未改** `gateway.py` / `config.py` / `.env`（不在 A5-1 涉改文件内）。建议后续把 `LLM_DOUBAO_MODEL` 改为可用模型（如 `doubao-seed-2-1-pro-260628`），或在 `.env` 覆盖

## A5-2 · 2026-07-19 16:40

- **改了什么**：
  - 🆕 `backend/app/schemas/onboarding.py` — `OnboardingRunRequest`（enterprise/brand/stores/services/competitors/seed_facts/target_engines/search_enabled 等）
  - `backend/app/api/v1/user/onboarding.py` — `POST /run` 接完整 body；BG task 改调 `runner.run_graph()`；新增 `_write_side_effects` 写 Brand/Store/Service + `enterprises.settings.target_engines`；保留 `GET /status` / `GET /snapshot`；去掉旧硬编码诊断→方案包流水线
  - `agent_task_service.py` — 未改（`create_or_reuse` 可选，现有 `create` 足够）
- **验收结果**：
  1. `from app.schemas.onboarding import OnboardingRunRequest` → `schema OK`
  2. `OnboardingRunRequest(...).model_dump()` → `validation OK`
  3. `from app.api.v1.user.onboarding import router` → `router OK`
- **遇到问题**：无（控制台中文乱码为 Windows 编码显示问题，不影响校验）

## A5-3 · 2026-07-19 16:42

- **改了什么**：
  - `backend/app/api/v1/user/onboarding.py` — 新增 `_bootstrap_kb()`：seed_facts → FactService；raw_inputs/pain_points → SignalService；`thin_kb_check()`；生成 llms.txt + `hosted_page_url` 占位写入 `enterprises.settings`；BG task 在 `_write_side_effects` 之后调用
  - `schemas/onboarding.py` — 只读引用 SeedFact，未改
- **验收结果**：
  1. `from app.api.v1.user.onboarding import _bootstrap_kb` → `import OK`
  2. FactService.create / SignalService.create 可访问 → `OK`
  3. `thin_kb_check(db, 99999)` → `passed: False` → `OK`
- **遇到问题**：无

## A5-4 · 2026-07-19 16:45

- **改了什么**：
  - 🆕 `backend/tests/a_track/test_a5_onboarding.py` — T-A5-01～06（schema / POST /run / diagnose+search / 三节点链 / 副作用落库 / KB bootstrap）；mock LLM + mock `faiss_sync_item.delay`；`run_graph` patch 路径为 `app.agents.runner.run_graph`（与 `_bg` 内 import 对齐）
- **验收结果**：
  1. `pytest tests/a_track/test_a5_onboarding.py -v` → **6 passed**
  2. a_track 全量（A0–A5）→ **35 passed** in 42.71s
- **遇到问题**：无（骨架里 schema 拆成 2 个函数会变成 7 条；合并为 1 条以满足「6 passed」）

# A3 执行记录 · LLM Gateway × 4 EngineAdapter

## A3-1 · 2026-07-19 12:26

- **改了什么**：
  - `backend/app/core/llm/schemas.py`：新增 `SearchCitation` / `SearchRequest` / `SearchResponse`
  - `backend/app/core/llm/base.py`：新增可选方法 `search()`，默认返回「不支持联网搜索」
- **验收结果**：
  - `python -c "from app.core.llm.schemas import ..."` → `schemas OK`
  - `python -c "from app.core.llm.base import BaseEngineAdapter"` → `base OK`
  - DoubaoAdapter 默认 search → `default: [doubao] 不支持联网搜索`
- **遇到问题**：无

## A3-2 · 2026-07-19 12:30

- **改了什么**：
  - `backend/app/core/llm/adapters.py`：`DoubaoAdapter.search()` 调 Responses API + 解析 citations；`request.model` 为空时强制 `doubao-seed-2-1-pro-260628`（决策 1B）
  - `backend/app/core/llm/gateway.py`：新增 `search()` / `simple_search()`，真搜失败后 chat 模拟并标 `simulated=true`
  - `backend/app/core/llm/__init__.py`：导出 Search* + search / simple_search
- **验收结果**：
  - import OK
  - `simple_search('静安寺皮肤管理推荐')` → query 正确、timeout=200
  - `test_doubao.py` → HTTP 200，citations=7
  - `search(engine='deepseek')` 有豆包 Key 时落到真实豆包搜索（engine=doubao, cites=4）——符合 gateway 降级链
  - 补充 mock：全引擎 search 不支持 + deepseek 有 Key → `simulated=True`（决策 2B 路径）
- **遇到问题**：step 验收写「DeepSeek → simulated=true」，但本机同时有豆包 Key，会先命中豆包真搜；chat 模拟用 mock 验证通过

## A3-3 · 2026-07-19 12:35

- **改了什么**：无（只读验证）
- **验收结果**：
  - 命令1 All imports OK
  - 命令2 OK — 4 engines registered（deepseek/doubao/kimi/wenxin）
  - 命令3 OK — fallback order correct
  - 命令4 OK — simple_prompt + simple_search work
  - 代码核对：4 Adapter ENGINE_CODE/_load_config ✅；ENGINE_ORDER=["deepseek","doubao","kimi","wenxin"] ✅；`_do_request` @retry 3次指数退避 ✅；embed 成功判定含 vectors 非空 ✅
  - **configured 备注**：doubao=true；deepseek/kimi/wenxin=false（`.env` 中 deepseek key 为空串，kimi/wenxin 未配）→ gateway.chat() 会 skip 未配置引擎
- **遇到问题**：无

## A3-4 · 2026-07-19 12:40

- **改了什么**：
  - 新建 `backend/tests/a_track/test_a3_gateway.py`：T-A3-01~06
  - T-A3-04 按决策 2B：有 Key + search 不支持 → `simulated=true`（非「无 Key」）
- **验收结果**：
  - `pytest tests/a_track/test_a3_gateway.py -v` → **6 passed**
  - `pytest tests/a_track/test_a0_industry_pack.py tests/a_track/test_a1_auth.py tests/a_track/test_a3_gateway.py -v` → **18 passed**
- **遇到问题**：无

---

## 🔍 A3 验收（审查者）· 2026-07-19

### 审查者自检 3 条

- [x] **直觉对不对** — ✅ 改动合理。
  - schemas：SearchCitation/SearchRequest/SearchResponse 设计清晰，字段与豆包 Responses API 对齐
  - base.py：search() 默认返回"不支持"，符合逐步覆盖模式
  - adapters.py：DoubaoAdapter.search() 正确走 `POST /responses`，citations 解析逻辑正确
  - gateway.py：search() 两级降级（真搜 → chat 模拟 + simulated=true）逻辑完整
  - `__init__.py`：导出完整，无遗漏
- [x] **实际跑通没** — ✅ `pytest tests/a_track/test_a0_industry_pack.py tests/a_track/test_a1_auth.py tests/a_track/test_a3_gateway.py -v` → **18 passed**
- [x] **越权没** — ✅ git diff 文件 == step §2 涉改文件清单：
  - `backend/app/core/llm/schemas.py` ✅ A3-1 §2
  - `backend/app/core/llm/base.py` ✅ A3-1 §2
  - `backend/app/core/llm/adapters.py` ✅ A3-2 §2
  - `backend/app/core/llm/gateway.py` ✅ A3-2 §2
  - `backend/app/core/llm/__init__.py` ✅ A3-2 §2
  - `backend/tests/a_track/test_a3_gateway.py` ✅ A3-4 §2
  - 未发现越权改动，🔍 文件未被碰

### 审查结论：**全部通过 ✅**

A3（LLM Gateway × 4 EngineAdapter）验收通过。6 条测试全 PASS，无越权改动，无破坏已有测试。

下一步：更新进度表 A3 → `done`，NOTIFY B（B1/B3 可接 chat() 真调）。

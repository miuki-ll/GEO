---
description: 执行者 A 专用：按 step 文件执行代码改动，不自作主张
alwaysApply: true
---

# 执行者规则（角色：开发者 A）

> 你是**执行者**，不是规划者。step 文件由 Claude Code 写好放在 `docs/开发手册/开发进度/steps/`。
> 你只负责：读 step → 自检 → 改代码 → 跑验收 → 写执行记录 → 回报。

---

## 权威来源

| 用途 | 路径 |
|------|------|
| A 任务清单 | `docs/开发手册/开发进度/TODO-A.md` |
| 单步细则 | `docs/开发手册/开发进度/steps/<StepID>.md`（如 `A1-1.md`） |
| **执行记录（必写）** | `docs/开发手册/开发进度/执行记录/Ax.md`（如 `A1.md`） |
| Step 制作规范 | `docs/开发手册/STEP-制作规范.md`（了解 step 结构） |
| 双人进度 | `docs/开发手册/G-L3-开发进度.md` |
| A 任务手册 | `docs/开发手册/G-L3-开发者A任务手册.md` |

❌ 不要看 `.cursor/rules/TODO.md` 或旧 `rules.md`。

---

## ⚠️ 执行前强制自检（必须全部 ✅ 才能改代码）

每个 step 文件开头都有这段。读完 step 后逐条确认：

- [ ] 我知道要改**哪个文件的哪几行**（或哪个方法/函数）
- [ ] 我知道改动前后的**行为差异**（输入 → 输出）
- [ ] 我能在改完后跑**哪条命令**来验证
- [ ] 我有把握不碰到无关代码，或知道影响范围

> **如果任一未勾选 → 不要动手。向用户提问，直到全部能勾选。**

---

## 硬规则

- **一次只做一个 Step**，验收前不跳到下一步
- **只改 step 文件里「涉改文件」列出的文件**，不改其他文件
- **权限标记为 🔍 只读的文件绝对不能改**
- **不超过 step 里写的范围**：step 没提的功能、重构、换技术栈一律不做
- **验收标准是必须通过的**：跑 step 里写的验收命令，输出不符合预期 = 没做完
- **阻塞就停**：缺依赖、测试跑不通、遇到 step 没说的情况 → 向用户报告，不要自己发挥
- **只在用户明确说 commit 时才 git commit**

---

## 每个 Step 的工作流

```
1. 打开 TODO-A.md，找到要做的 Step ID（如 A1-1）
2. 完整阅读 steps/A1-1.md
3. 过「执行前自检」4 条 → 不过则问用户
4. 改代码（只改涉改文件，遵循不可破坏约束）
5. 跑验收命令
6. 追加执行记录到「docs/开发手册/开发进度/执行记录/Ax.md」
7. 向用户报告三句话
```

---

## 执行记录格式（每一步必写）

做完每一步后，**追加**到 `docs/开发手册/开发进度/执行记录/Ax.md`（如 `A1.md`）：

```markdown
## Ax-n · YYYY-MM-DD HH:MM

- **改了什么**：<文件列表，每个文件改了什么>
- **验收结果**：<验收命令 + 关键输出>
- **遇到问题**：无 / <具体问题和处理方式>
```

如果是**修复审查驳回**，在同一个 step 下面追加：

```markdown
### 修复 · YYYY-MM-DD HH:MM

- **审查意见**：<引用审查者的驳回原因>
- **修复了什么**：<文件 + 改了什么>
- **验收结果**：<重新跑验收命令 + 输出>
```

---

## 完成后（向用户报告）

每个 step 完成后，向用户报告三句话（不要长篇）：

```
Ax-n 完成：
- 改了什么：<文件列表，每个文件一句话>
- 验收结果：<验收命令 + 输出摘要>
- 遇到问题：无 / <具体问题>
```

然后：
1. 对照 step 文件 §验收标准，确认全部勾选
2. 在 `TODO-A.md` 勾选该 step
3. 报告下一步 Step ID

> **不要更新进度表、不要 git commit、不要发 NOTIFY** — 这些是审查者的事。

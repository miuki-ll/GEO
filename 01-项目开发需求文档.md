# GEO 多企业 AI 可见度运营平台 — 项目开发需求文档

> 版本：v1.0 | 日期：2026-07-04 | 状态：基线定稿
> 来源：PRD v4.0 | 生美（beauty_local）行业 MVP

---

## 1. 项目概述

### 1.1 产品定位

多企业 GEO（Generative Engine Optimization）SaaS 平台。在 AI 引擎（豆包/DeepSeek/Kimi/文心）成为用户选购参考入口的时代，GEO 帮助企业确保其品牌信息能出现在 AI 的回答中，并获得 AI 信任。

**核心公式**：内容价值 ≈ 信息增量 × 品牌信任度
**北极星指标**：AI 信任资产 → 分渠道可见度 → 转化闭环

### 1.2 差异化优势

| 维度 | 说明 |
|------|------|
| KB 零幻觉 | 知识库事实可追溯，杜绝虚构品牌 |
| Scenario 分渠道 | 以真实问答场景驱动，非批量铺量 |
| Core/Probe 监测 | 双轨监测体系，Core 持续跟进，Probe 探测新信源 |
| 七段转化闭环 | 从知识录入到转化迭代的完整闭环 |
| 双人工闸门 | 方案包确认 + 草稿审，确保质量可控 |

### 1.3 目标用户

- **生美行业商户**（美容院、皮肤管理中心等本地生活商家）
- **代运营机构**（管理多个门店/品牌的 GEO 策略）
- **品牌总部**（连锁品牌统一管理各地门店可见度）

### 1.4 核心概念

| 概念 | 说明 |
|------|------|
| GEO | Generative Engine Optimization，生成式引擎优化 |
| KB（知识库） | 企业知识库，分 Fact/Signal/External 三层 |
| Scenario | 最小生产单元，一个真实问答场景 |
| Skill | 内容技能类型，与渠道绑定 |
| Core | 主监测池，持续跟踪已选 scenario 的 AI 提及 |
| Probe | 探针池，探测新信源和未覆盖的 AI 渠道 |
| Strategy Pack | 方案包，包含画像/竞品/场景/渠道的完整策略 |

---

## 2. 技术栈与架构

### 2.1 技术选型总表

| 层级 | 选型 |
|------|------|
| 前端框架 | Vue 3 + Vite + TypeScript + Element Plus + Pinia |
| 后端框架 | Python 3.11+ FastAPI Pydantic v2 全量校验 |
| 数据层 | PostgreSQL 15+ / Redis 7 / Celery / Faiss / OSS |
| LLM 层 | packages/llm/gateway + 4 EngineAdapter |
| Agent 架构 | L1 Jobs+Celery / L2 LangGraph 子图 / L3 ReAct Harness |
| 可观测性 | Sentry / LangSmith / agent_traces |
| 部署 | Docker Compose / ECS + RDS |

### 2.2 系统架构分层

`
+-- 前端应用（Vue3）-------------------------+
| 三舱 UI | 管理后台 | Dashboard | 审核工作台 |
+-----------------------+---------------------+
                        | REST API (FastAPI)
+-----------------------+---------------------+
|         API 网关层                        |
| 认证/RBAC | 路由 | 限流 | 请求校验       |
+-----------------------+---------------------+
                        |
+-----------------------+---------------------+
|           业务服务层                       |
| 租户/RBAC | KB | 诊断/策略 | 内容        |
| 审核 | 发布 | 监测 | 效果舱              |
+-----------------------+---------------------+
                        |
+-----------------------+---------------------+
|         Agent 编排层                       |
| L1 Celery / L2 LangGraph / L3 ReAct      |
+-----------------------+---------------------+
                        |
+-----------------------+---------------------+
|         LLM Gateway 层                    |
| 豆包/DeepSeek/Kimi/文心 适配              |
+-----------------------+---------------------+
                        |
+-----------------------+---------------------+
|     数据层 PostgreSQL/Redis/Faiss/OSS     |
+--------------------------------------------+
`

### 2.3 Agent 三层架构

- **L1**：通用 LLM / LLM Gateway — MVP
- **L2**：Jobs + LangGraph + ReAct — MVP
- **L3**：行业包 + Skill + KB + 三舱 — 壁垒·重心

正文 Skill 特点：固定链 kb_fetch → LLM → fact_verify，不含 ReAct。

---

## 3. 模块划分

| 编号 | 模块 | MVP | 说明 |
|------|------|-----|------|
| M1 | 租户/RBAC | ✅ | 多企业隔离，角色权限 |
| M2 | 知识库 | ✅ | Fact/Signal/External 三层 |
| M3 | 诊断+策略+Agent | ✅ | 开店向导全链路 |
| M3-UI | 三舱 | ✅ | 懂我/定方案/出结果 |
| M4 | 内容生产 | ✅ | Scenario→Skill→Content 管线 |
| M5 | 审核系统 | ✅ | 机器审 + 人工审 |
| M6 | 发布引擎 | ✅ | 托管页 AUTO；小红书 SEMI |
| M7 | 监测体系 | ✅ | Core + Probe |
| M8 | 效果舱/报告 | ✅ | Dashboard |
| M9 | Beauty_local 行业包 | ✅ | 生美规则/模板/合规 |

---

## 4. 数据模型

核心表：enterprises / users / kb_facts / kb_faqs / kb_signals / kb_externals / target_engines / scenarios / content_drafts / publish_tasks / monitor_results / agent_tasks / outcome_snapshots

关键约束：
- 所有核心表带 enterprise_id 实现租户隔离
- fact_refs 可追溯
- kb_updated_at 租户级

---

## 5. API 设计

| 域 | 方法/路径 |
|----|-----------|
| 认证 | POST /api/v1/auth/login, /register |
| 企业 | GET/PUT /api/v1/enterprise/profile, /members/invite |
| 知识库 | CRUD /api/v1/kb/facts, /faqs, /signals |
| 向导 | POST /api/v1/onboarding/run, GET /status/{task_id} |
| 诊断 | GET /api/v1/diagnosis/pain, /persona, /competitor |
| 方案包 | GET /api/v1/strategy-pack/draft, POST /confirm |
| 内容 | CRUD /api/v1/content/drafts, POST bulk-approve |
| 发布 | CRUD /api/v1/publish/tasks, POST retry, /semi |
| 监测 | GET /api/v1/monitor/core, /probe, /trigger |
| 效果舱 | GET /api/v1/outcomes/dashboard, /ai-kpi, /traffic |
| Agent | POST /api/v1/agent/tasks, GET /{id} |

---

## 6. 前端 UI/UX

### 路由
/login /onboarding /strategy-pack /content/drafts /outcomes /knowledge-base /publish/tasks /monitor /settings

### 核心组件
OnboardingWizard / ProgressChain / StrategyPack / PersonaCard / CompetitorAnalysis / ScenarioSelector / ChannelWeightSlider / DraftWorkbench / PublishConsole / MonitorPanel / AIDashboard / KBMaintenance

### UX 原则
复杂分析封装在系统内；用户 2 次确认闸门；开店向导进度条；方案包一页展示；效果舱信息密集 Dashboard

---

## 7. Agent 编排引擎

**L1 Celery Jobs**：任务链编排、入参/出参、重试策略、进度追踪
**L2 LangGraph 子图**：诊断子图（DIAGNOSE→PAIN→PERSONA→COMPETITOR）、生产子图（GAP→PLAN→EXECUTE→机器审）
**L3 ReAct Harness**：正文 Skill 固定链（kb_fetch→LLM→fact_verify），不含 ReAct

---

## 8. LLM Gateway

统一接口（LLMRequest / LLMResponse），EngineAdapter 模式（4 引擎适配），Token 用量统计，回退策略（主→次→结构化错误）

---

## 9. 审核与发布

机器审核：fact_verify / compliance / beauty_compliance / 禁堆砌 / 禁虚构
人工审核：草稿工作台 → 通过/驳回
发布：托管页 AUTO；小红书/知乎 AUTO→SEMI；点评 GUIDED
约束：无 Fact 不发布；条数 ≤ scenario 数（≤5）；临界出草案

---

## 10. 监测体系

Core（~20 query/engine）按周轮询 → 提及率/信任度
Probe（≤10 query/engine）探测新信源 → 评估→入 Core 候选
2 周临界触发 → gap_analyze → 草案 → 人确认 → 回方案包

---

## 11. 版本路线

| 版本 | 范围 | 核心 |
|------|------|------|
| MVP | 生美 | 三舱/scenario/MVP-A/kb_fetch/Core-Probe |
| V1.1 | 生美增强 | trust_asset/hybrid_retrieve/全量Core/ConsultLog |
| V2 | B2B | adapt_engine/深度内容/完整Dashboard |
| V2+ | 偏好 | 排序/rerank/蒸馏(opt-in) |

**MVP-A 首发**：诊断主攻 engine 全量；scenario 2 个；托管页AUTO+1渠道SEMI；Core 1 轮

---

## 12. 验收标准

路径：开店向导 → 方案包确认 → FAQ+1小红书 → 托管页 → Core 监测 1 轮
验证：fact_refs 可追溯；禁词拦截；租户隔离；2 次闸门；AUTO失败可SEMI

---

## 13. 开发计划

| 阶段 | 周期 | 产出 |
|------|------|------|
| S1 基础设施 | 2天 | 脚手架/Docker/迁移 |
| S2 核心框架 | 3天 | 认证/KB CRUD/Gateway |
| S3 三舱UI | 4天 | 向导→方案包→效果舱 |
| S4 Agent流水线 | 5天 | 诊断链/生产管线/审核 |
| S5 发布+监测 | 4天 | 发布引擎/Core/Probe |
| S6 效果舱+迭代 | 3天 | Dashboard/临界/迭代 |
| S7 行业包+验收 | 3天 | Beauty_local/AC验收 |

**总计**：24天（约5个工作周）

---

## 14. 部署与目录结构

### 本地开发
Docker Compose: PostgreSQL 15 + Redis 7 + FastAPI + Celery Worker + Vue3

### 生产环境
阿里云 ECS + RDS PostgreSQL + Redis 托管 + OSS + Sentry + LangSmith

### 目录结构
`
geo-platform/
├── frontend/          # Vue3 + Vite + TS
│   └── src/
│       ├── api/       # API 封装
│       ├── components/
│       ├── views/     # onboarding/strategy/outcomes/kb/...
│       ├── stores/    # Pinia
│       └── router/
├── backend/           # Python FastAPI
│   ├── geo_core/
│   │   ├── api/v1/   # REST 路由
│   │   ├── services/ # 业务逻辑
│   │   ├── agents/   # L1/L2/L3
│   │   ├── llm/      # Gateway + Adapters
│   │   └── models/   # SQLAlchemy
│   ├── alembic/
│   └── Dockerfile
├── infra/
│   └── docker-compose.yml
└── docs/
`

---

*文档结束*

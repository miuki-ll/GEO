# =============================================================================
# GEO Backend — Python 3.11+ (tested on 3.13)
# Install: pip install -r requirements.txt
# =============================================================================

# --- Web / API ---
fastapi>=0.115.0,<1.0
uvicorn[standard]>=0.30.0,<1.0
python-multipart>=0.0.12,<1.0
aiofiles>=24.1.0,<26.0

# --- Config / Validation ---
pydantic>=2.9.0,<3.0
pydantic-settings>=2.5.0,<3.0
python-dotenv>=1.0.0,<2.0
email-validator>=2.2.0,<3.0

# --- Database ---
sqlalchemy>=2.0.35,<3.0
pymysql>=1.1.0,<2.0
alembic>=1.13.0,<2.0

# --- Auth / Security ---
python-jose[cryptography]>=3.3.0,<4.0
passlib[bcrypt]>=1.7.4,<2.0
argon2-cffi>=23.1.0,<26.0

# --- Task Queue ---
redis>=5.0.0,<6.0
celery>=5.4.0,<6.0

# --- HTTP / Retry ---
httpx>=0.27.0,<1.0
tenacity>=9.0.0,<10.0

# --- Rate Limiting ---
slowapi>=0.1.9,<1.0
limits>=3.13.0,<5.0

# --- Observability ---
sentry-sdk[fastapi,celery,sqlalchemy]>=2.16.0,<3.0
langsmith>=0.1.139,<1.0

# --- Vector Search / Data (V2+ KB RAG) ---
numpy>=1.26.0,<3.0
pandas>=2.2.0,<3.0
faiss-cpu>=1.9.0,<2.0

# --- MCP（可选）---
fastmcp>=2.0.0,<3.0

# --- Dev / Test ---
pytest>=8.3.0,<9.0
pytest-asyncio>=0.24.0,<1.0

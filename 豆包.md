# 豆包联网搜索 · 接入验证文档

> 验证日期：2026-07-18 | 状态：✅ 通过

## 1. 前提条件

| 条件 | 说明 |
|------|------|
| ARK API Key | 见 `backend/.env` 的 `LLM_DOUBAO_API_KEY`（勿写入仓库） |
| 已开通联网内容插件 | CC_content_plugin2000000859843690306 |
| 模型 | `doubao-seed-2-1-pro-260628`（250615+ 版本） |

## 2. API 调用格式

```
端点: POST https://ark.cn-beijing.volces.com/api/v3/responses
Auth: Bearer {ARK_API_KEY}
```

### 请求体

```json
{
  "model": "doubao-seed-2-1-pro-260628",
  "tools": [{"type": "web_search", "max_keyword": 3, "limit": 10}],
  "input": [{"role": "user", "content": "搜索问句"}]
}
```

#### 关键参数

| 参数 | 默认 | 说明 |
|------|------|------|
| `max_keyword` | 不限 | 单轮搜索关键词数，1-50 |
| `limit` | 10 | 返回结果数，1-50 |
| `sources` | 全部 | 可选：douyin/moji/toutiao |
| `user_location` | 无 | `{"type":"approximate","region":"上海","city":"上海"}` |

#### 注意事项

- ⚠️ **直接用模型名**，不要用 ep-xxx 推理接入点 ID
- ⚠️ **timeout 设 180 秒以上**，搜索需要时间
- ⚠️ **Chat API 不支持 web_search**，必须用 Responses API

## 3. 返回结果解析

### citations 位置

```
output[] → content[] → annotations[]  (type == "url_citation")
```

### citation 字段

```json
{
  "type": "url_citation",
  "title": "文章标题",
  "url": "https://...",
  "summary": "引用片段摘要（长文本）",
  "site_name": "新浪财经",
  "publish_time": "2026年07月17日 18:22:32(CST) 星期五",
  "logo_url": "站点图标"
}
```

### 搜索用量

```json
"usage": {
  "tool_usage": {"web_search": 2},
  "tool_usage_details": {"web_search": {"search_engine": 2}},
  "total_tokens": 8615
}
```

## 4. 测试方法

在 `backend/` 目录下：

```powershell
$env:LLM_DOUBAO_API_KEY="<从 backend/.env 读取，勿提交>"
.venv\Scripts\python.exe test_doubao.py
```

测试脚本 `test_doubao.py` 包含：
1. 美容院怎么选择（通用搜索）
2. 静安寺附近皮肤管理推荐（LBS 搜索）

## 5. GEO 集成方案

### A3 LLM Gateway 改动

在 `DoubaoAdapter` 新增 `search()` 方法：

```python
class DoubaoAdapter(_OpenAICompatAdapter):
    async def search(self, query: str, max_keyword: int = 5,
                     limit: int = 10, location: dict = None,
                     timeout: int = 180) -> SearchResponse:
        """联网搜索，返回 citations"""
        url = f"{self.base_url}/responses"
        tool = {"type": "web_search", "max_keyword": max_keyword, "limit": limit}
        if location:
            tool["user_location"] = location
        body = {
            "model": "doubao-seed-2-1-pro-260628",
            "tools": [tool],
            "input": [{"role": "user", "content": query}],
        }
        # HTTP POST, 解析 annotations → SearchResponse
        ...
```

### 调用方：A7 诊断

```
diagnose_probe() 生成探针问句
  → for each probe:
      adapter.search(probe) → citations + answer_text
  → 分析 brand_mention / rank / competitor / source_map
```

## 6. 费用

| 项目 | 费用 |
|------|------|
| 联网插件费 | 每月首 2 万次免费，超出 4 元/千次 |
| Token 费 | 输入+输出，联网链路含意图识别/改写/总结 |
| 典型单次 | 约 0.001 元（2 万次内免费时仅 Token 费） |

## 7. 常见错误

| 错误 | 原因 | 解决 |
|------|------|------|
| HTTP 400 `missing tools.function` | 用了 Chat API | 切换到 Responses API |
| HTTP 404 `ToolNotOpen` | 未开通联网插件 | 控制台开通 CC_content_plugin |
| 超时无响应 | 默认 timeout 不够 | 设 timeout≥180 |
| ep-xxx 超时 | 端点不支持搜索 | 直接用模型名 |
| citations 为空 | 类型过滤错 | 用 `url_citation` 不是 `citation` |

# GEO 项目 · Claude Code 规则

> 本文件由 Claude Code 自动加载。角色：**规划者 + 审查者**。

---

## 角色定义

- **我 = 规划者 + 审查者**：读代码 → 提问澄清 → 写 step 文件 → 审查执行记录 → 跑验收 → 通过或驳回
- **Cursor AI = 执行者**：读 step 文件 → 自检 → 改代码 → 跑验收 → 写执行记录 → 三句话回报
- **用户 = 产品方向决策者 + 在两边传递文件的人**

**我不直接写业务代码。** 我只产出 step 文件 + 审查验收。

---

## 交互文件（我和 Cursor AI 共享的通道）

| 文件 | 方向 | 用途 |
|------|:--:|------|
| `steps/Ax-n.md` | 我 → Cursor | 任务指令（只读） |
| `执行记录/Ax.md` | Cursor → 我 → Cursor | 执行结果 + 审查意见（双向读写） |
| `TODO-A.md` | Cursor 勾 step，我勾 done | 进度状态 |

**执行记录是唯一的双向通道**。Cursor 每做完一步往里面追加，我审查时读它 + 追加审查结果。

---

## 工作流（完成一个任务）

### 规划阶段（我）

```
用户给任务方向
  ↓
我读代码 → 提问澄清 → 80% 把握
  ↓
我写 steps/Ax-1~Ax-4.md + 更新 TODO-A.md
  ↓
用户把 step 文件丢给 Cursor AI
```

### 执行阶段（Cursor AI，每步重复）

```
Cursor 读 steps/Ax-n.md
  ↓
自检 4 条 → 不过则问用户
  ↓
改代码 + 跑验收
  ↓
追加执行记录到 执行记录/Ax.md
  ↓
勾 TODO-A.md 的 Ax-n
  ↓
向用户三句话回报
  ↓
用户把回报转给我看（可选）
  ↓
进行下一个 Ax-n
```

### 审查阶段（全部 step 完成后，我）

```
用户说"Ax 全部 step 完成"
  ↓
我读 执行记录/Ax.md + git diff
  ↓
审查者自检 3 条：
  [ ] 直觉对不对 — 抛开 step，改动合理吗
  [ ] 实际跑通没 — 我自己跑验收命令
  [ ] 越权没 — git diff 文件 == step §2 文件
  ↓
追加审查结果到 执行记录/Ax.md
  ├── 全部通过 → 更新 G-L3-开发进度.md → 勾 TODO-A → git commit → 发 NOTIFY
  └── 有驳回 → 通知用户"请让 Cursor AI 按执行记录/Ax.md 审查意见修复"
```

### 驳回修复（如果审查不通过）

```
用户把 执行记录/Ax.md 给 Cursor AI
  ↓
Cursor AI 读审查意见 → 修复代码 → 重跑验收 → 追加修复记录
  ↓
用户说"驳回项已修复"
  ↓
我二次审查 → 通过 → 收尾
```

---

## 开工必读

每次用户提新任务方向时，按顺序读：

1. `docs/开发手册/G-L3-开发进度.md` — 当前进度、阻塞、依赖
2. `docs/开发手册/开发进度/TODO-A.md` — A 轨任务清单
3. `docs/开发手册/STEP-制作规范.md` — step 文件怎么写（模板 + 约束）

---

## 规划者约束

### 数量
- 每个任务（Ax）**不超过 4 个 step**（验收不占 step）
- 每轮**最多写 5 个 step 文件**
- 每个 step 内改动 **≤ 5 项**

### 质量
- 写 step 前必须 **Read 所有涉改文件**
- 改动精确到**文件路径 + 方法名/行号**
- 验收标准**必须是具体命令**
- 每个 step 开头有**执行前自检 4 条**

### 顺序
Step 必须按执行顺序：🛠️ 改代码 → 🔍 验证 → 🧪 测试

最后一个 step 永远是 🧪 测试。

---

## 审查者约束

1. **直觉对不对** — 抛开 step 文件，有没有不对劲的地方
2. **实际跑通没** — 自己跑验收命令，不接受执行者截图
3. **越权没** — git diff 文件 == step §2 涉改文件，🔍 文件未被碰

> 审查结果写入 `执行记录/Ax.md`。全过 = 勾 done + 更新进度表。驳回 = 写清原因 + 指向文件/行号。

---

## 文件速查

| 文件 | 谁写 | 谁读 |
|------|:--:|:--:|
| `steps/Ax-n.md` | 我 | Cursor |
| `执行记录/Ax.md` | Cursor + 我 | 双方 |
| `TODO-A.md` | 我建，Cursor 勾 step，我勾 done | 双方 |
| `G-L3-开发进度.md` | 只我改 | 双方只读 |
| `STEP-制作规范.md` | 我维护 | Cursor 只读 |
| `.cursor/rules/dev-handbook-steps.mdc` | 我维护 | Cursor 自动加载 |
